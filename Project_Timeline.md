# Project Timeline — Meme Creation & Sharing Platform

This is the living build plan: phases in build order, each with an **exit test** that must pass before the next phase starts. See `Project_Requirements.md` for what each feature must do, and `CLAUDE.md` (root/backend/frontend) for how it's built. Durations are t-shirt sizes, not calendar promises — nobody has real velocity data for this project yet. Fill in actual dates as each phase starts/finishes.

**Rule of the sequence**: a phase isn't "done" because the code is written — it's done when its exit test passes on a real device/API call, not just unit tests in isolation. Don't start the next phase's build until the current one's exit test is green. If a phase turns up a design gap, fix it in `Project_Requirements.md` before continuing, don't quietly improvise around it in code.

**Update cadence**: update the status column + add a Daily Log entry (bottom of this file) at the end of every work day — what moved, what's blocked, what's next. Stale timeline is worse than no timeline.

**How to test**: every phase's entry below must include a "How to test" subsection once that phase's build is done and flipped to `Testing` — concrete steps (which screen/button, what to enter) and the expected output/result for each step, written for the user to follow directly on the running app, not just "hit the exit test." Add this in the same changeset that flips a phase to `Testing`, not as an afterthought.

## Status legend
`Not started` · `In progress` · `Blocked` · `Testing` · `Done`

## Phase index

| # | Phase | Size | Status | Started | Finished |
|---|-------|------|--------|---------|----------|
| 0 | Project scaffolding | S | Done | — | — |
| 1 | Auth & profile | S | Done | 2026-07-20 | 2026-07-21 |
| 2 | Friends | S | Done | 2026-07-21 | 2026-07-21 |
| 3 | Public feed (Public/Friends audiences only) | M | Done | 2026-07-21 | 2026-07-21 |
| 4 | Meme creator + global templates | M | Done | 2026-07-21 | 2026-07-21 |
| 5 | Communities core (create/join/leave, privacy) | M | Done | 2026-07-21 | 2026-07-21 |
| 6 | Community-private templates | S | Done | 2026-07-21 | 2026-07-21 |
| 7 | Multi-audience posting (+ community) | M | Done | 2026-07-21 | 2026-07-21 |
| 8 | Scoring stub + leaderboards (individual, global community, internal community) | M | Done | 2026-07-21 | 2026-07-22 |
| 9 | Global competitions (Meme of Day/Week/Month) | S | Done | 2026-07-22 | 2026-07-22 |
| 10 | Community challenges — intra-community team vs. team | L | Done | 2026-07-22 | 2026-07-22 |
| 11 | Community challenges — community vs. community | M | Done | 2026-07-22 | 2026-07-22 |
| 12 | Real-time meme sending + inbox | M | Testing | 2026-07-22 | |
| 13 | AI caption/joke generator | S | Done | 2026-07-23 | 2026-07-23 |
| 14 | Sharing system | S | Testing | 2026-07-23 | |
| 15 | Instagram Companion Mode | M | Testing | 2026-07-23 | |
| 16 | Hardening pass (security, perf, accessibility, full regression) | M | Testing | 2026-07-23 | |
| 17 | Redis/arq infrastructure (scoring, leaderboards, challenges, ai-caption, instagram — replacing live-SQL/in-process stopgaps) | M | Testing | 2026-07-23 | |

---

## Phase 0 — Project scaffolding
**Goal**: backend and frontend projects exist, boot, and talk to a real Postgres instance.
**Deliverables**: FastAPI skeleton (SQLAlchemy + Alembic wired to Postgres), Expo RN skeleton with the agreed folder structure, both `CLAUDE.md`s reflecting the real stack.
**Exit test**: `uvicorn` boots the FastAPI app against a live Postgres `DATABASE_URL`; `alembic upgrade head` runs clean on an empty DB; Expo app builds and launches on a device/simulator to the default screen.
**Status**: Done (completed in an earlier session).

## Phase 1 — Auth & profile
**Goal**: a user can register, log in, and see their own profile — the identity every later phase depends on.
**Deliverables**: `User` model + migration, JWT issue/verify, register/login/me endpoints, RN login/register screens, session held in a Redux slice.
**Exit test**: register a new account via the app, log out, log back in, see the correct profile on the Profile screen; a request to a protected endpoint without a token is rejected (401).
**Note (2026-07-20)**: backend fully verified (automated tests + manual curl, real Postgres). Frontend type-checks and bundles clean (`tsc`, `expo export --platform web`).
**Note (2026-07-21)**: user manually tapped through register → logout → login → profile against live `uvicorn` + `expo start --web` dev servers — confirmed working. Exit test green. See `.claude/memory/auth-profile.md` for full detail.

## Phase 2 — Friends
**Goal**: the Friends relationship exists and is provably separate from community membership (needed before Phase 3's audience filtering).
**Deliverables**: `friendship` model (request/accept/remove), friends list/request endpoints, RN friends screen.
**Exit test**: user A sends a friend request to user B, B accepts, both see each other in their friends list; a third user C does not see A/B as friends.
**Note (2026-07-21)**: started without Phase 1's manual on-device gate being green (user's explicit call — see Daily Log). Backend fully verified (16 new pytest, 22/22 total, against real Postgres; migration applied to the dev DB). Frontend type-checks and bundles clean (`tsc`, `expo export --platform web`). User then manually tapped through both Phase 1 and Phase 2 flows together against live dev servers — friend request send/accept/mutual-visibility confirmed working. Exit test green. See `.claude/memory/friends.md` for full detail.

## Phase 3 — Public feed (Public/Friends audiences only)
**Goal**: the core feed loop works before communities exist to complicate it — Community audience is added later in Phase 7 on purpose, so audience filtering is provable in isolation first.
**Deliverables**: `Meme`/`post_audience` models, upload endpoint (image, no text overlay editor yet — server just stores the image + caption text), infinite-scroll feed endpoint filtered by audience, reactions + comments, RN feed screen with `FlatList`.
**Exit test**: post as Public — a logged-out-of-friendship third user can see it; post as Friends-only — only accepted friends see it, a non-friend does not; reactions/comments round-trip correctly.
**Note (2026-07-21)**: media storage decided as Cloudinary (user's choice — credentials added to `backend/.env`). Backend: `Meme`/`PostAudience`/`Reaction`/`Comment` models, keyset-paginated feed with server-side audience visibility (reused Phase 2's `Friendship` model directly), migration applied to the real dev DB; 38/38 pytest passing total (16 new, Cloudinary calls mocked in tests) against real Postgres; Cloudinary integration additionally smoke-tested for real via a manual multipart upload — confirmed a live, publicly-reachable `res.cloudinary.com` URL. Frontend: `FeedScreen` (infinite scroll + pull-to-refresh), `NewPostScreen` (minimal upload UI — no text overlay/templates, that's Phase 4), inline-expandable comments on each card; `tsc`/`expo export --platform web` both clean (8 routes). See `.claude/memory/meme-feed.md` for full detail, including two real bugs hit and fixed along the way (an async relationship-loading gotcha and a FastAPI multipart-form gotcha — both documented there for future phases).
**Note (2026-07-21, cont'd)**: user manually tapped through Phase 3's exit test on live dev servers — Public/Friends visibility and reactions/comments confirmed working. Exit test green, phase flipped to Done.

## Phase 4 — Meme creator + global templates
**Goal**: the actual creation tool — upload, text overlay, template picker, preview/publish — works end to end using only the global template library (community templates don't exist until Phase 6).
**Deliverables**: `Template` model (global scope only for now), text overlay editor (gesture-handler + reanimated), camera/gallery picker, preview screen, publish wired to the Phase 3 feed endpoint.
**Exit test**: pick a template, add top/bottom text, preview matches the final published post pixel-for-pixel (same overlay position/text), publish succeeds and appears in the feed from Phase 3.
**Note (2026-07-21)**: Backend: `Template` model (global scope, no `community_id` yet — that's Phase 6) + `/templates` create/list endpoints, reusing Phase 3's Cloudinary upload path via a newly-extracted shared `services/media.py` (promoted since templates is the 2nd consumer of image-upload validation) and a shared `core/pagination.py` cursor helper (2nd consumer of Phase 3's keyset cursor scheme). Migration applied to the real dev DB; 46/46 backend pytest passing total (8 new for templates, Cloudinary mocked — mock consolidated into `conftest.py`). Frontend: new `features/creator/` replaces the old minimal `NewPostScreen` at the same `/new-post` route — pick own image or a template (grid picker modal with inline template-upload), drag top/bottom text overlay on an `OverlayCanvas` (`react-native-gesture-handler` + `react-native-reanimated`, both newly wired up via a new `GestureHandlerRootView` in `_layout.tsx`), **Preview** captures the composed view to a real PNG via newly-added `react-native-view-shot`, then caption + Public/Friends audience selection, then **Publish** uploads that exact captured file through the unchanged Phase 3 `/memes` endpoint — preview and published post are pixel-identical by construction (same file), not by comparison. `tsc`, `expo export --platform web` (8 routes, unchanged), and `expo lint` (bootstrapped ESLint for the first time in this repo as a side effect — 0 errors, 1 pre-existing React Compiler/`react-hook-form` warning) all clean. Seeded a real global template (user-supplied `meme_template.jpg`, uploaded via the real `/templates` endpoint, not a raw file copy). Editor scope explicitly confirmed with user: top/bottom draggable text only, no arbitrary text boxes/stickers — intended design, not a gap.
**Note (2026-07-21, cont'd)**: hit and fixed a stale-server bug during manual testing — an orphaned `uvicorn --reload` worker process (parent already killed, child survived — a Windows-specific quirk, since killing a process there doesn't cascade to children) kept serving pre-Phase-4 routes, causing `/templates` to 404 even though the code was correct. Killed the orphaned process tree and restarted cleanly; documented in `.claude/memory/meme-creator.md` as a gotcha for future phases. User then manually tapped through Phase 4 on live dev servers and confirmed it works. Exit test green, phase flipped to Done.

## Phase 5 — Communities core
**Goal**: communities exist as a first-class entity with real membership and access control, before anything (templates, feeds, leaderboards) is scoped to them.
**Deliverables**: `Community`/`community_membership` models, owner-set privacy (`open`/`invite_only`), create/join/leave/invite-approve endpoints, RN Communities tab (My Communities / Discover) + Community Detail shell (Members tab only for now).
**Exit test**: create an open community, a second user joins with no approval step; create an invite-only community, a join request requires owner approval before membership is granted; a non-member cannot hit any member-only endpoint for that community (403, not just hidden in UI).
**Note (2026-07-21, cont'd)**: user tapped through Phase 5's exit test on live dev servers across two sessions — open-community join confirmed instant, invite-only join confirmed pending-then-owner-approved, and a non-member correctly got a 403 (not just hidden UI) trying to view an invite-only community's members, which the detail screen surfaced gracefully as inline text rather than a crash. Along the way fixed two real frontend bugs (both documented in `.claude/memory/communities.md`): a broken `communities/index.tsx` + `communities/[id].tsx` folder co-location that made Expo Router swallow `/communities` navigation into the dynamic route (`id="index"`, sent as a literal non-UUID to the backend), fixed by moving the list screen to a flat `communities.tsx`; and a noisy `console.error` in `services/api.ts::throwApiError` that fired for every gracefully-handled error (including expected 403s), removed since the existing `console.warn` network monitor already covers debugging needs without looking like a crash. Exit test green, phase flipped to Done.
**Note (2026-07-21)**: Backend: `Community`/`CommunityMembership` models (`owner`/`member` roles only — no `admin` yet, nothing to promote to it in this phase), `/communities` endpoints — create (auto-adds owner as an active member), discover (paginated, all communities regardless of privacy — existence is public per Requirements §3, only member-only *content* is gated), mine (unpaginated), single-get, join (open→immediate, invite-only→pending), leave (owner blocked, 400), members (open→public, invite-only→members-only, 403 otherwise), join-requests list/approve/reject (owner-only). Migration applied to the real dev DB; 61/61 backend pytest passing total (15 new). Frontend: `features/communities/` — Communities tab (My Communities/Discover) reachable from the profile screen, Create Community screen (name/description/privacy/optional icon), Community Detail screen (join/leave/pending button that switches on `viewer_membership_status`, owner-only join-requests approve/reject section, members list). First folder-based nested route group in the app (`app/communities/{index,new,[id]}.tsx`) — hit an Expo Router typed-routes quirk (bare `/communities` isn't a valid typed href, only `/communities/index`; dynamic `[id]` navigation needs the object form, not a template string), documented in `.claude/memory/communities.md`. `tsc`, `expo export --platform web` (11 routes), `expo lint` (0 errors) all clean. Hit the same orphaned-`uvicorn`-worker issue as Phase 4 (twice) — restarted cleanly each time. No human tap-through on a simulator/device yet.

## Phase 6 — Community-private templates
**Goal**: prove the "private to members, invisible to everyone else" rule on the simplest possible feature before it's load-bearing for posting/challenges.
**Deliverables**: `template.community_id`, community template upload/browse endpoints, RN Templates tab split (global vs. this community's private set).
**Note (2026-07-21)**: Backend: nullable `template.community_id` (FK communities) added via migration; `POST /templates` gained an optional `community_id` form field, gated by a new shared `services/communities.py::require_active_membership` helper (404 if the community doesn't exist, 403 if not an active member — reused as-is, no per-caller duplication); the global `GET /templates` list now explicitly excludes community-scoped rows; a new `GET /communities/{id}/templates` browse endpoint is member-gated **with no open-community exception** (stricter than Phase 5's member-list rule — community templates are private to members full stop, per Requirements §3). Migration applied to the real dev DB; 67/67 backend pytest passing (6 new). Frontend: `TemplatePickerModal` (used by the Phase 4 creator) gained a horizontal scope-tab row — Global plus one tab per community the user belongs to (via `useMyCommunities`) — switching between `useTemplates()` and a new `useCommunityTemplates(communityId)`; extracted the shared grid UI into `TemplateGrid.tsx` so both scopes render identically without duplicating the `FlatList`. The existing "+" upload affordance now targets whichever scope is active. `tsc`, `expo export --platform web` (11 routes, unchanged), `expo lint` (0 errors) all clean. No human tap-through yet.
**Exit test**: a member uploads a community template — a non-member's global template list does not include it, and a direct API request for it as a non-member is rejected; a member sees and can apply it in the creator.
**Note (2026-07-21, cont'd)**: user manually tapped through Phase 6's exit test on live dev servers — a member uploaded a community template, a non-member's global template list confirmed to exclude it, a direct API request as a non-member confirmed rejected (403), and a member confirmed able to see and apply it in the creator. Exit test green, phase flipped to Done.

## Phase 7 — Community posting (+ public exposure for open communities)
**Goal**: give communities a real posting surface, now that Phase 5 gives them something to post into.
**Deliverables**: a **community post** flow reachable only from inside a community (not a generic creator audience option), backend audience derived automatically from the community's `open`/`invite_only` privacy (open → also public, with the community shown alongside the poster; invite-only → community-only), community feed endpoint/screen, backend membership validation (can't post to a community you're not in).
**Exit test**: post in an **open** community — it shows in both that community's feed and the public feed, with the community visibly attached to the post (not just the poster); post in an **invite-only** community — it shows only in that community's feed, never the public feed; attempting to post to a community the user isn't a member of is rejected server-side even if the client is tampered with.
**Design history (2026-07-21)**: built twice. **First pass** (superseded, do not resurrect): a single generic creator let a poster multi-select Public/Friends/one-or-more-communities in one publish action (client-chosen `community_ids` sent to `POST /memes`) — this matched the *original* `Project_Requirements.md` §4 wording. The user then explicitly redirected mid-phase: community posting must be its own flow, only reachable from inside a community, with community visibility fully **automatic** from the community's privacy (no manual audience choice, no multi-community cross-posting). `Project_Requirements.md` §3/§4/§17 were rewritten to match; treat those sections, not this timeline entry, as the source of truth for current scope. **Second (final) pass**: Backend — new `POST /communities/{id}/memes` (member-gated, no client-chosen audience; auto-adds a `community` `PostAudience` row plus a `public` row iff the community is `open`) alongside the original `POST /memes` (reverted to Public/Friends-only, rejects a literal `"community"` in `audiences`). `require_active_membership` now returns the `Community` row (needed for `.privacy`). `MemeOut` gained a `community: {id, name} | null` badge (replacing an interim `community_ids: uuid[]` list field from the first pass) so feed cards can show "posted by X in community Y." `PostAudience` still carries the `community` enum value + nullable `community_id` FK from the first pass (no migration change needed for the redesign — only Python-level service/router logic changed). 76/76 backend pytest passing. Frontend — `CreatorScreen` now branches on an optional `communityId`/`communityName` route param: personal-post mode keeps the Public/Friends chips; community-post mode shows a static "Posting to `<name>`" panel and no picker at all, submitting via a new `useCreateCommunityMemeMutation`. Entry point is a "+ Post" button in `CommunityDetailScreen`'s Feed tab (active members only) — there is no community picker anywhere in the generic creator. `MemeCard` shows the community badge under the author's name. `tsc`, `expo export --platform web` (11 routes), `expo lint` (0 errors) all clean. No human tap-through yet.

## Phase 8 — Scoring stub + leaderboards
**Goal**: get the three read-only leaderboard surfaces working against a deliberately simple placeholder scoring formula, without waiting on the full scoring-engine design (see `Project_Requirements.md` §7).
**Deliverables**: `services/scoring.py` stub (e.g. likes + comments, clearly labeled placeholder), background recompute worker, individual leaderboard, global community leaderboard, internal per-community leaderboard, RN Leaderboards tab + community-scoped leaderboard tab.
**Exit test**: all three leaderboards rank correctly against known seeded data; internal community leaderboard is only reachable/visible to that community's members; **no endpoint accepts a write to any leaderboard** — they are read paths only.
**Note (2026-07-21)**: Backend: `services/scoring.py` — `meme_score_expr()`, a SQLAlchemy scalar-subquery stub (`reactions*1 + comments*2`), clearly labeled placeholder per §7, computed **live in SQL** (no stored score table) since the inputs are cheap owned-row aggregations — no staleness possible, so no recompute worker was needed for this stub (see below). `services/leaderboards.py` — three read-only queries: `GET /leaderboards/individual` (all users, all-time, all their memes), `GET /leaderboards/communities` (all communities, ranked by aggregate community-post score, visible to everyone per the discovery/content-visibility split), `GET /communities/{id}/leaderboard` (that community's active members only, ranked by community-post-only score, member-gated via the existing `require_active_membership`). Offset-based `page`/`limit` pagination (not the feed's keyset cursor scheme — score ranking has no stable resume-row). 84/84 backend pytest passing (8 new). Frontend: new `features/leaderboards/` — `LeaderboardsScreen` (Individual/Communities tab toggle, route `/leaderboards`, reachable from a new button on `SessionScreen`); `CommunityDetailScreen` gained a third `Leaderboard` tab alongside Feed/Members, member-gated the same way. Shared `IndividualLeaderboardRow`/`CommunityLeaderboardRow` components. `tsc`, `expo export --platform web` (12 routes), `expo lint` (0 errors, same 2 pre-existing warnings) all clean.
**Background-worker decision**: no Celery/arq/task-queue infra exists in this repo yet (`workers/` is an empty planned folder; every prior phase's I/O has run inline). Discussed with user — chose **recompute inline (i.e., live SQL on read)** over standing up real worker infra this phase, since the stub formula's inputs are trivially cheap aggregations with no staleness window. Revisit once the real scoring engine (§7, still deferred) has non-trivial inputs that can't be computed live on every leaderboard read — that's the point a stored `meme_scores` table + a real scheduled worker becomes necessary, likely alongside Phase 12's Redis infra. See `.claude/memory/scoring-engine.md`.
**Blocked**: nothing. No human tap-through yet on Phase 8's exit test.

## Phase 9 — Global competitions (Meme of the Day/Week/Month)
**Goal**: the one-vote-per-period competition layer, independent of community challenges.
**Deliverables**: `Vote` model with the `(user_id, meme_id, period)` unique constraint, vote endpoint, period-close worker, RN voting UI + winner display.
**Exit test**: a user's first vote in a period succeeds, a second vote in the same period is rejected by the DB constraint (not just the API layer); at period close, the correct winner is surfaced.
**Note (2026-07-22)**: Backend: `Vote` model (`user_id`, `meme_id`, `period_type` enum day/week/month, `period_key` string) with `UniqueConstraint(user_id, meme_id, period_type, period_key)` — **per backend/CLAUDE.md's directive verbatim**, confirmed explicitly with user over the looser "one vote per period" prose in `Project_Requirements.md` §9 (the two read as in tension; user chose the literal constraint, which blocks re-voting the *same* meme twice in a period but permits voting for several different memes within one period). `services/competitions.py` — deterministic `period_key()`/`period_bounds()` (day `YYYY-MM-DD`, week ISO `YYYY-Www`, month `YYYY-MM`, all UTC), `cast_vote` (400 if the target meme isn't `public`-audience — competitions are public-feed-level only per §9; 409 on duplicate), `get_current_standings` (live in-progress tally), `get_winner` (400 if the period hasn't closed yet, since votes could still flip it; `meme: null` if zero votes). **No period-close worker was built** — same precedent as Phase 8's scoring stub: no Celery/arq infra exists yet, a period's vote tally is a cheap live aggregation, so "closed" is just `now >= period_end` checked at read time. Discussed with user and confirmed before building (see `.claude/memory/voting-system.md`). New `/competitions` router (vote/current/winner, all Bearer-auth-gated matching the leaderboards convention). Promoted `memes.py`'s private `_build_meme_out` to a public `build_meme_out` (2nd real consumer). 93/93 backend pytest passing (9 new), migration applied to the real dev DB. Frontend: `features/voting/VotingScreen.tsx` (route `/voting`, reachable from `SessionScreen`) — day/week/month tabs, live standings list with inline vote buttons, a `WinnerBanner` showing the most recently closed period's winner (client computes "previous period" key via a new `utils/competitionPeriods.ts`, first consumer of the newly-added `date-fns` dependency, mirroring the backend's exact period-key scheme). `tsc`, `expo export --platform web` (13 routes), `expo lint` (0 errors, same 2 pre-existing warnings) all clean. Hit and worked around an Expo Router gotcha: `.expo/types/router.d.ts` (typed routes) isn't regenerated by `expo export`, only by the dev server actually bundling — had to briefly run `expo start --web` and hit it once before `tsc` would recognize the new `/voting` route (documented in `.claude/memory/voting-system.md`).
**Note (2026-07-22, cont'd)**: user confirmed Phase 9's exit test tested and verified working — flipped to Done.

## Phase 10 — Community challenges: intra-community team vs. team
**Goal**: the first full challenge lifecycle (setup → active window → evaluation → results/rewards) in its simplest shape, one community, two internal sides.
**Deliverables**: `challenge`/`challenge_participant`/`challenge_submission` models, challenge setup/submit endpoints, scheduled window-close + evaluation worker (using the Phase 8 scoring stub), points + badge award, RN challenge setup wizard + active/results screens.
**Exit test**: set up a short test-length challenge (minutes, not days, for testability), two sides submit memes, a submission attempted after window close is rejected, evaluation picks the correct winner per the scoring stub, winner's members receive points + a badge visible on their profile.
**Note (2026-07-22)**: Two explicit user decisions before building (see `.claude/memory/challenges.md`): (1) window close is a **scheduled worker**, not live-SQL-on-read like Phase 8/9 — backend/CLAUDE.md is explicit that the window-close moment must be a single consistent event, so this phase stood up the repo's first background worker (`app/workers/challenges.py`, a minimal in-process `asyncio` polling loop on a 5s interval, started via FastAPI's `lifespan`, single-process only — not Celery/arq, which stays deferred until a feature actually needs real task-queue infra); (2) sides/members are **creator-assigned at setup time** (owner picks 2+ named sides and assigns members to each in one call), no self-signup endpoint. Challenge creation is **owner-only**, matching the existing join-request-approval precedent. Backend: `Challenge`/`ChallengeSide`/`ChallengeParticipant`/`ChallengeSubmission`/`Badge` models (the first two have a genuine circular FK — `challenges.winning_side_id` → `challenge_sides.id` and back — resolved via a named `use_alter=True` constraint on the model + a matching manually-reordered migration); `services/challenges.py` (setup validation, submission window/ownership checks, `evaluate_challenge` scoring each side via the Phase 8 `meme_score_expr()` stub, no-winner-on-a-true-tie, points+badge award to the winning side's members only); new `/communities/{id}/challenges/*` router + `GET /auth/me/badges`. 107/107 backend pytest passing (12 new), migration applied to the real dev DB. Frontend: `features/challenges/CreateChallengeScreen.tsx` (fixed 2-side setup, minutes-based duration input, member picker with mutually-exclusive side toggling) + `ChallengeDetailScreen.tsx` (branches on status: active shows a submission picker sourced from the community feed filtered to the caller's own memes — no new "my memes" endpoint added — evaluated shows a winner banner + full submissions list), a 5s poll on the challenge-detail query while `status === "active"` so the worker's server-side status flip surfaces without a manual refresh, a new Challenges tab on `CommunityDetailScreen`. `tsc`, `expo export --platform web` (15 routes), `expo lint` (0 errors, same 2 pre-existing warnings) all clean.
**Blocked**: nothing. Also fixed a real 404 bug reported after this phase shipped — the running dev `uvicorn --reload` worker predated the challenges router and never picked it up (same orphaned-worker gotcha as earlier phases); killed and restarted cleanly, confirmed via `curl .../openapi.json` that the routes were actually live. User is tap-testing Phase 10 and 11 together.

**How to test**:
1. Open a community you **own** (create one first if needed — Communities tab → Create). Add at least 2 other accounts as members (join with a second/third test account).
2. Go to that community → **Challenges** tab. You should see two buttons: **"+ Team Challenge"** and **"Challenge a Community"** (only visible to the owner).
3. Tap **"+ Team Challenge"**. Enter a title, a short duration (e.g. `2` minutes), name Side A/Side B, and assign at least one member to each side (tapping a member on one side removes them from the other). Tap **Start challenge**.
   - *Expected*: navigates to the challenge detail screen, status pill shows **ACTIVE**, both sides listed with member counts.
4. As a member assigned to a side, post a meme in that community (Feed tab → **+ Post**), then go back to the challenge and tap it under **Submit a meme for `<side name>`**.
   - *Expected*: the meme appears greyed out with "Submitted" once tapped; submitting the same meme again isn't offered.
5. As a member **not** assigned to any side, open the same challenge.
   - *Expected*: "You are not assigned to a side in this challenge" — no submission picker shown.
6. Wait past the challenge's end time (or set a very short duration like 1–2 minutes to test quickly). Try submitting a meme after it ends (e.g. re-open before the worker has evaluated but after `end_time`).
   - *Expected*: a red error message rejecting the submission (window closed).
7. Wait ~5–10 seconds after the window closes (the background worker polls every 5s) and refresh/re-open the challenge.
   - *Expected*: status flips to **EVALUATED** automatically (no manual action needed), a winner banner shows ("🏆 `<side>` wins!" or "Tie — no winner" if scores were equal), and a **Submissions** list appears below with each submitted meme, its submitter, and side.
8. As a winning-side member, check `GET /auth/me/badges` (or ask to have this checked) — should show a new `challenge_winner` badge with `points: 100`. A losing-side member should have no new badge from this challenge.

## Phase 11 — Community challenges: community vs. community
**Goal**: the second challenge shape, reusing Phase 10's lifecycle machinery scoped to two communities instead of two sides within one.
**Deliverables**: challenge type variant for cross-community, matching UI entry point.
**Exit test**: two communities' members submit during the window, aggregate community performance determines the winner, both communities' leaderboard standing (Phase 8) updates accordingly.
**Note (2026-07-22)**: One explicit user decision before building (see `.claude/memory/challenges.md`): **propose/accept, not propose/auto-active** — either community's owner can propose a challenge against another community, but it only goes `active` once the *opponent* community's owner accepts (prevents one community unilaterally dragging another's leaderboard into a challenge). Backend: added `Challenge.opponent_community_id` (new migration) + `ChallengeSide.community_id` set on both sides (reusing the nullable column Phase 10 already reserved for this); `services/challenges.py` gained `propose_challenge`/`accept_challenge`/`decline_challenge` (owner-of-home / owner-of-opponent gated) alongside Phase 10's `create_challenge`, and `submit_to_challenge`/`evaluate_challenge` now branch on `challenge_type` — `community_vs_community` has no `ChallengeParticipant` roster, so a submitter's side is derived live from `CommunityMembership`, and a winning side's badge recipients are that community's **entire active membership**, not a pre-picked sub-roster. A `community_vs_community` submission must target a meme already posted as a `community` post to the submitter's side-community — this is what makes results flow into that community's existing global-community-leaderboard score (Phase 8) with no separate scoring path, satisfying the exit test's "both communities' leaderboard standing updates accordingly" without new plumbing. New routes: `POST .../challenges/vs/{opponent_id}`, `POST .../challenges/{id}/accept`, `DELETE .../challenges/{id}/decline`; `list_community_challenges` now returns challenges where the community is either the home or the opponent. 118/118 backend pytest passing (11 new in `test_vs_challenges.py`), migration applied to the real dev DB. Frontend: `features/challenges/ProposeVsChallengeScreen.tsx` (route `/communities/[id]/challenges/vs`, owner-only, opponent picked from `useDiscoverCommunities`) + `ChallengeDetailScreen.tsx` extended to show a pending-proposal state with Accept/Decline buttons (visible only to the opponent's owner) and to resolve "my side" via community membership instead of a member roster for this shape; a second owner-only button ("Challenge a Community") added next to Phase 10's "+ Team Challenge" on the Challenges tab. `tsc`, `expo export --platform web` (16 routes), `expo lint` (0 errors, same 2 pre-existing warnings) all clean.
**Blocked**: nothing. No human tap-through yet on Phase 11's exit test — user is testing Phase 10 and 11 together. Also note: this feature requires an Expo dev-server **and** backend restart after the code landed (both had the same stale-reload gotcha — see Gotchas in `.claude/memory/challenges.md`) before it became reachable in the app; if a newly-built feature seems "missing" after this, restarting both dev servers is the first thing to check, not the code.

**How to test**:
1. Create two communities owned by **different accounts** (e.g. account A owns "Home", account B owns "Opponent"), each with at least one other member so there's someone besides the owner to check badge awards on.
2. As account A (owner of Home), go to Home → **Challenges** tab → tap **"Challenge a Community"**. Enter a title, a short duration (e.g. `2` minutes), and pick "Opponent" from the list. Tap **Send challenge**.
   - *Expected*: navigates to the challenge detail screen, status pill shows **"Awaiting response"** (grey), a note saying "Waiting for the challenged community's owner to respond."
3. Still as account A, go to Opponent's Challenges tab (if A is also a member) or just note the challenge won't show an Accept/Decline option for A — only B's owner gets that.
4. Switch to account B (owner of Opponent). Go to Opponent → **Challenges** tab — the same challenge should be listed there too (status "Pending"), since both communities see a proposal in their list. Open it.
   - *Expected*: **Accept** and **Decline** buttons are visible (only to B, the opponent's owner).
5. Tap **Accept**.
   - *Expected*: status flips to **ACTIVE** for everyone viewing it (the account A screen should pick this up within ~5 seconds via polling, no manual refresh needed).
6. As a member of Home, post a meme **into the Home community** (not a personal/public post — must use the community's **+ Post**, since only community-posted memes are eligible here) and submit it to the challenge. Do the same as a member of Opponent, posting into the Opponent community.
   - *Expected*: submitting a **personal-feed** (non-community) meme to this challenge is rejected with an error — only community-posted memes for your own side qualify.
7. Wait for the window to close (~5–10 seconds after `end_time`, worker polling).
   - *Expected*: status flips to **EVALUATED**, winner banner shows the winning community's name, submissions list shows both communities' entries.
8. Check badges for a member of the winning community who **never personally submitted anything** (e.g. a plain member, not the owner).
   - *Expected*: they still receive a `challenge_winner` badge — Phase 11 awards the **entire winning community's active membership**, not just people who submitted.
9. Check the **Communities leaderboard** (Leaderboards tab → Communities) — the winning community's score should reflect the submitted memes' reactions/comments same as any other community post, since challenge submissions are ordinary community posts under the hood.
10. Separately, test **Decline**: propose a second challenge, have the opponent owner tap **Decline** instead of Accept.
    - *Expected*: `204` success, the challenge disappears from both communities' Challenges lists entirely (not marked "declined" — it's deleted).

## Phase 12 — Real-time meme sending + inbox
**Goal**: friend-to-friend real-time sending, independent of the feed/community system.
**Deliverables**: WebSocket connection manager, inbox fallback row for offline recipients, reaction-only replies, RN inbox drawer + socket-status Redux slice.
**Exit test**: two connected clients send/receive a meme in real time with no page refresh; one recipient goes offline, receives the meme as an inbox entry, and sees it correctly on reconnect.

**Note (2026-07-22)**: Backend: `MemeSend` model (`sender_id`/`recipient_id`/`meme_id`, `status` enum `pending`/`delivered`/`seen`, nullable `reaction`) + migration applied to the real dev DB; in-memory `ConnectionManager` (`app/websockets/connection_manager.py`, single-process `dict[user_id, WebSocket]` — same no-Celery/arq-yet precedent as the Phase 10 challenge worker, revisit with Redis pub/sub only if the app ever runs more than one API process); `/meme-sending/send` (friend-gated via a new `services/friends.py::are_friends` helper, tries live delivery through the connection manager and falls back to a persisted `pending` inbox row if the recipient isn't connected — never assumes the recipient is online), `/meme-sending/inbox` + `/sent`, `/inbox/{id}/seen` (recipient-only ack), `/inbox/{id}/react` (recipient-only, reaction-only reply — pushes a `meme_send_reaction` frame back to the sender if connected); `WS /meme-sending/ws?token=<jwt>` (JWT via query param — the one deliberate exception to the Bearer-header convention, since browsers can't set headers on a WS upgrade). Extracted `services/memes.py::get_meme_out_for_viewer` (shared single-meme query with live reaction/comment counts, reused so the embedded meme in a `MemeSendOut` isn't a stale/zeroed snapshot). 125/125 backend pytest passing (7 new, including a real end-to-end `TestClient.websocket_connect` test proving live delivery with no polling). Frontend: `store/socketSlice.ts` (connection status), `services/memeSendingSocket.ts` (single WebSocket connection manager singleton, 3s auto-reconnect on any close), `services/useMemeSending.ts` (TanStack Query hooks + a socket-message-to-cache-invalidation sync hook), `features/meme-sending/SendMemeModal.tsx` (friend picker, opened from a new "↗ Send" button on `MemeCard`), `features/meme-sending/InboxScreen.tsx` (route `/inbox`, live socket-status dot, auto-ack-on-view, 4 fixed emoji quick-reactions — no free-text chat, matching reaction-only scope). Socket lifecycle wired into `app/_layout.tsx`'s `AuthBoundary`, keyed off the auth token (connect on login, disconnect on logout). `tsc`, `expo export --platform web` (17 routes), `expo lint` (0 errors, same 2 pre-existing warnings) all clean. See `.claude/memory/meme-sending.md` for full detail, including a real WS-auth gotcha hit and fixed (the WS route must inject `db` via `Depends(get_db_session)`, not call `async_session_factory()` directly, or it silently bypasses the test DB override).
**Blocked**: nothing. No human tap-through yet on Phase 12's exit test.

**How to test**:
1. Log in as two different accounts on two separate clients/tabs (e.g. two `expo start --web` browser tabs, or web + a simulator) that are already friends (use the Friends tab to send/accept a request first if not).
2. On account A's feed, find any meme and tap **"↗ Send"** under it. Pick account B from the friend list and tap **Send**.
   - *Expected*: the row shows "Sent" immediately; no error.
3. On account B's client — **without refreshing the page/app** — open **Inbox** (button on the profile/session screen).
   - *Expected*: if B was already sitting on the Inbox screen or anywhere in the app with the socket connected (check the small status dot next to "Inbox" — should be green "connected"), the new meme should already be there or appear the moment it's sent, with no manual refresh. This is the real-time part of the exit test.
4. Tap one of the 4 emoji reactions under the received meme on B's client.
   - *Expected*: the emoji highlights as selected.
5. Switch back to account A's client and open **Inbox → Sent** (or just re-check via the API/UI if a "sent" view isn't directly exposed) — actually check A's `GET /meme-sending/sent` reflects B's reaction, or simplest: just confirm no error occurred: A's client should reflect the reaction if A is also connected (socket pushes `meme_send_reaction` back to the sender live).
6. Now close/background account B's client entirely (simulating offline), and send another meme from A to B.
   - *Expected*: the send still succeeds (`201`) with `status: "pending"` rather than `"delivered"` — it didn't fail just because B is offline.
7. Reopen account B's client and navigate to **Inbox**.
   - *Expected*: the meme sent while B was offline is now visible in the inbox list, correctly, with no data loss — this is the "receives it as an inbox entry, sees it correctly on reconnect" half of the exit test.

## Phase 13 — AI caption/joke generator
**Goal**: LLM-assisted captioning bolted onto the existing creator, fully backgrounded so it can never block publish.
**Deliverables**: `integrations/llm_client.py`, background task with timeout/retry, "make it funnier" iteration endpoint, RN caption generator UI in the creator.
**Exit test**: request a caption, receive a suggestion within the configured timeout; trigger a provider failure/timeout (mocked) and confirm the creator still lets the user publish manually rather than hanging or crashing.
**Note (2026-07-23)**: Provider decision: user chose **Groq** (only option with no key already present in `backend/.env`); model pinned to `llama-3.1-8b-instant` per explicit instruction to use whichever burns token/rate limits slowest — smallest/cheapest/highest-throughput model in Groq's catalog, plenty for short caption suggestions. Real `GROQ_API_KEY` added directly to `backend/.env` (gitignored, not committed). Backend: `app/integrations/llm_client.py::generate_caption` calls Groq's OpenAI-compatible `/openai/v1/chat/completions` endpoint directly via `httpx` (already a dependency — no new SDK), 8s timeout, one retry on timeout/network/5xx (not on 4xx, which fails fast), raises `LLMGenerationError` on final failure. `services/ai_caption.py::generate_meme_caption` builds a first-draft vs. "make it funnier" prompt depending on whether `current_caption` is provided, translates `LLMGenerationError` → `CaptionGenerationFailedError` (502). New `POST /ai-caption/generate` router, registered in `main.py`. **No background task queue was used** despite the deliverable wording — this is a synchronous call inside the request, since the endpoint itself is optional and separate from publish (see rationale in `.claude/memory/ai-caption.md`); "never block publish" is satisfied structurally (two independent mutations client-side), not via async task deferral. 134/134 backend pytest passing (9 new: 5 endpoint-level with the service mocked, 4 hitting `llm_client.generate_caption` directly with a mocked `httpx.AsyncClient.post` to prove the retry/4xx-no-retry logic for real, no live network calls in the suite). Frontend: `services/aiCaption.ts` + `services/useAiCaption.ts` (`useGenerateCaptionMutation`), wired into the **existing** `CreatorScreen.tsx` caption step — a "✨ Generate a caption"/"✨ Make it funnier" button (label depends on whether the caption field already has text) fills `caption` via `setValue` on success; a failure only shows an inline error, Publish stays fully independent and unaffected. `tsc`, `expo lint` (0 errors, same 2 pre-existing warnings), `expo export --platform web` (17 routes, unchanged) all clean. No human tap-through yet — needs a real Groq call against the live key to confirm end-to-end.

**How to test**:
1. Start creating a meme (Creator screen — pick an image or template, optionally add top/bottom overlay text).
2. Tap **Preview** to reach the caption/publish step.
3. Tap **"✨ Generate a caption"** (shown when the caption field is empty).
   - *Expected*: button shows "Thinking…" briefly, then the caption field fills with an AI-generated caption relevant to your top/bottom text (or a generic funny caption if you left both blank).
4. Edit the generated caption slightly, or leave it, then tap the button again — it should now read **"✨ Make it funnier"**.
   - *Expected*: a new suggestion appears that's a riff on your *current* caption text (iteration), not a totally unrelated fresh caption.
5. Tap **Publish** immediately after generating a caption (or without generating one at all).
   - *Expected*: publishes normally either way — caption generation and publish are fully independent actions.
6. To confirm the failure path doesn't hang/crash the creator: temporarily set `GROQ_API_KEY` in `backend/.env` to an invalid value, restart `uvicorn`, tap "✨ Generate a caption".
   - *Expected*: after a few seconds, an inline red error appears ("Couldn't generate a caption right now…") — the app does not freeze, crash, or block the Publish button, which still works to publish manually. Restore the real key and restart `uvicorn` afterward.

## Phase 14 — Sharing system
**Goal**: get memes out of the app onto other platforms.
**Deliverables**: native share sheet integration, image/video export.
**Exit test**: share sheet opens with the correct target apps on both iOS and Android; exported file matches the published meme (image/text overlay intact).
**Scope note (2026-07-23)**: confirmed with user — **image-only**, no video export. `Project_Requirements.md` §12's "image/video" wording doesn't apply here since no video meme pipeline exists anywhere in the app yet (image-only `Meme` model, no video upload path) — building video export now would mean inventing an unused pipeline. Revisit if/when a video meme feature is ever added.
**Note (2026-07-23)**: Frontend-only phase, no backend changes. New dependencies `expo-sharing`/`expo-file-system` (SDK-57-compatible versions via `npx expo install`). `features/sharing/shareMeme.ts::shareMemeImage(imageUrl, memeId)` downloads the meme's remote Cloudinary image to the OS cache dir via `expo-file-system`'s new SDK-57 `File`/`Paths` API, then opens the native OS share sheet via `expo-sharing`'s `shareAsync` — per `frontend/CLAUDE.md`'s explicit "never a custom share modal" directive, WhatsApp/Instagram/X aren't hardcoded targets, the OS resolves them. A "⤴ Share" button was added directly to the existing `MemeCard.tsx` action row (next to "↗ Send"), with local `isSharing`/`shareError` state — no new screen, no Redux (single-component transient state). "Export as image" is satisfied by the same native share sheet, since both Android's and iOS's sheets include a built-in "Save Image" target — no separate save-to-gallery button/dependency was added. `tsc`/`expo lint` (0 errors, same 2 pre-existing warnings)/`expo export --platform web` (17 routes, unchanged) all clean. **No automated tests added** — this feature is a thin wrapper directly over two native Expo module calls with no branching business logic beyond try/catch, real verification is the device tap-through (see `.claude/memory/sharing.md` for the reasoning). No human tap-through yet — `expo-sharing` is limited/no-op on web, so this needs an actual Android/iOS device or simulator, not `expo start --web`, to mean anything.

**How to test** (requires a real Android/iOS device or simulator — the OS share sheet doesn't meaningfully work in `expo start --web`):
1. Open the Feed (or a community feed) and find any meme.
2. Tap **"⤴ Share"** under the meme.
   - *Expected*: brief loading spinner (downloading the image), then the native OS share sheet opens with app icons for WhatsApp/Instagram/X/etc. (whichever are installed) plus a "Save Image"/"Copy" style system target.
3. Pick "Save Image" (or platform equivalent).
   - *Expected*: the image saves to the device's photo gallery, matching the published meme (overlay text intact — it's the same flattened image that was uploaded, not a re-render).
4. Pick WhatsApp (or another installed app) instead.
   - *Expected*: the app opens with the meme image attached/ready to send, same as sharing any photo from the device's gallery.
5. Test with airplane mode or network briefly disabled, then tap Share.
   - *Expected*: an inline red error appears under the meme (e.g. "Could not download the meme to share.") — no crash, no infinite spinner.
6. Repeat steps 2-4 on the other OS (Android if you tested iOS first, or vice versa) — the exit test explicitly requires both.

## Phase 15 — Instagram Companion Mode
**Goal**: wrap external Reels/posts as first-class, competition-eligible content without re-hosting video.
**Deliverables**: `MemeContainer` model, share-to-app intake, oEmbed/link metadata fetch (background task), WebView display, independent reactions/comments, feed-level competition eligibility.
**Exit test**: share a Reel link into the app, a `MemeContainer` is created with correct metadata/thumbnail, it appears in the public feed and can be voted on (Phase 9), and — confirmed per `Project_Requirements.md` — it **cannot** be submitted as a challenge entry (Phase 10/11 submission endpoint rejects it).
**Scope decisions (2026-07-23, confirmed with user before building)**: (1) only externally-shared content gets containerized — native uploads (`Meme`) are never wrapped; (2) native memes and `MemeContainer`s coexist in **one** merged public feed, not two separate feeds, even though they're backed by entirely separate tables/reactions/comments/votes under the hood; (3) Instagram's real oEmbed/Graph API needs app-review credentials this project doesn't have, so `integrations/instagram_oembed.py::fetch_metadata()` is a pluggable stub with a stable interface (same pattern as the Phase 8 scoring stub) — swap the body for a real call later without touching any caller; (4) the metadata fetch runs as a **fire-and-forget `asyncio.create_task`** at intake, not inline-synchronous (would risk blocking intake on a slow external call) and not a scheduled worker (this is one-shot per-container enrichment, not recurring — simpler than Phase 10's challenge-close worker).
**Note (2026-07-23)**: Backend: `MemeContainer` model + three parallel tables (`ContainerReaction`/`ContainerComment`/`ContainerVote`) mirroring `Reaction`/`Comment`/`Vote` but scoped to `meme_container_id` — chosen over retrofitting polymorphism onto the existing meme-scoped tables, which are hard-FK'd to `memes.id` throughout Phases 3/8/9/10 and would have required touching every one of those phases' queries. New `/instagram/containers/*` endpoints (create/get/react/unreact/comment/list-comments). `GET /memes/feed`'s **response shape changed**: now a tagged-union `MergedFeedPage` (`{kind: "meme"|"container", meme|container}`), computed via a single SQL `UNION ALL` across `Meme` and `MemeContainer` (not a Python-side merge of two full scans) — `GET /communities/{id}/feed` is unchanged, containers aren't community-scoped. Competitions extended: new `POST /competitions/{period}/container-votes/{id}` parallel to the existing meme-vote endpoint (no self-vote restriction, no public-audience gate — a container has neither concept); `StandingEntry`/`WinnerOut` **response shape changed** to a `content: {kind, meme}|{kind, container}` union so both content types rank together in one list. Challenge-submission rejection of containers needed **zero new code** — `submit_to_challenge` looks up `Meme.author_id`, which no container ID will ever match, so it 400s by construction (verified with a dedicated test). 144/144 backend pytest passing (10 new), migration applied to the real dev DB (required a hand-patch post-autogenerate: reusing the existing `competition_period_type` Postgres enum for `ContainerVote.period_type` needed `postgresql.ENUM(..., create_type=False)` in the migration file itself, since Alembic's autogenerated DDL doesn't infer that flag from the model — documented as a reusable gotcha in `.claude/memory/instagram-companion.md`). Frontend: `features/instagram-companion/` — `ContainerCard.tsx` (feed card with a `react-native-webview` `WebView` pointed at the source URL directly, since the stubbed backend has no real oEmbed HTML yet, plus an "Open Original ↗" link via the already-installed `expo-web-browser`), `ContainerCommentsSection.tsx`, `ShareInstagramLinkModal.tsx` (a plain modal off a new "Share Reel" button on `FeedScreen`, no new route — same pattern as `SendMemeModal`). `MemeFeedList.tsx` gained a `MergedFeedList` export used by `FeedScreen`; `CommunityDetailScreen`'s community feed is untouched. `VotingScreen`/`StandingRow`/`WinnerBanner` updated to branch on the new `content.kind` union. New dependency: `react-native-webview`. `tsc`, `expo lint` (0 errors, same 2 pre-existing warnings), `expo export --platform web` (17 routes, unchanged — no new route) all clean. No human tap-through yet.

**How to test**:
1. Open the Feed and tap **"Share Reel"** (next to "New Post").
2. Paste an `instagram.com` link (e.g. `https://www.instagram.com/reel/<anything>/`) and tap **Share**.
   - *Expected*: modal closes, the feed refreshes, and a new card appears showing "shared from Instagram" with a WebView loading the link, plus "Loading details…" text.
3. Wait a few seconds and pull-to-refresh (or just wait — the container auto-polls while pending).
   - *Expected*: "Loading details…" is replaced by a title line (the stubbed metadata fetcher always succeeds with a placeholder title derived from the URL, so this should flip from pending to filled-in within a couple seconds, never stay stuck).
4. Try pasting a non-Instagram link (e.g. a TikTok URL).
   - *Expected*: inline validation error, share is rejected before hitting the network.
5. On the new container card, tap the ♡ reaction and the comment count to open comments; add a comment.
   - *Expected*: reaction/comment counts update independently of any native meme's counts — confirm by checking a nearby native meme card's counts don't change.
6. Tap **"🏆 Vote"** on the container card.
   - *Expected*: flips to "Voted", same as voting on a native meme. Go to **Voting** tab (Meme of the Day) — the container should appear in the Today standings ranked by vote count, shown with an "IG" placeholder thumbnail if no real thumbnail was fetched.
7. Tap **"Open Original ↗"** on the container card.
   - *Expected*: opens the actual Instagram link in an in-app browser (not the WebView preview).
8. Try to submit the shared container as a challenge entry (via any active community challenge's submission flow, if you have one running) using the container's ID.
   - *Expected*: rejected — a container is never a valid challenge submission target. (This is primarily an API-level guarantee; there's no UI path that would let you pick a container for a challenge submission in the first place, since the submission picker only ever lists your own native memes.)

## Phase 16 — Hardening pass
**Goal**: a full regression across every permission/audience/scoring surface before calling the MVP done, plus the non-functional work that's easy to skip mid-feature.
**Deliverables**: security review (auth, audience/community access checks, rate limiting on write endpoints), performance pass on feed/leaderboard/challenge queries under seeded load, accessibility pass on core screens (labels, touch targets, screen reader), full cross-phase regression.
**Exit test**: security review has no open criticals; leaderboard/feed endpoints hold acceptable latency under seeded load; accessibility audit passes on Feed, Creator, Communities, Leaderboards, Profile; every exit test from Phases 1–15 still passes in sequence on a clean environment.
**Note (2026-07-23)**: Full audit + fix pass across all four sub-areas — see `.claude/memory/hardening.md` for complete detail.
- **Security**: audited every router/service for auth gaps, audience/community access-control gaps, and IDOR risk. Found and fixed one real IDOR: `meme_sending.py::send_meme` resolved the target meme via a raw unchecked `db.get()` instead of the visibility-checked `get_visible_meme` — a user could forward a Friends-only or private-community meme they couldn't otherwise see, just by knowing its ID. Added rate limiting for the first time in this repo (`slowapi`, Redis-backed — the first real Redis usage anywhere in the codebase; the `redis` Python package had been listed in requirements since Phase 0 but never actually installed/exercised) on every high-abuse-risk write endpoint (auth register/login, AI caption generation, votes, meme upload, reactions/comments, meme sending, Instagram container creation), keyed by authenticated user ID (IP-keyed only for pre-auth endpoints). Tightened CORS from a dev-only wildcard to an explicit, env-driven origin allow-list.
- **Performance**: reviewed indexes on every table in the feed/leaderboard/challenge/competition hot paths. Added 7 missing indexes via a new migration — most notably `memes.created_at` (the feed's primary sort key had no index at all) and a composite covering `community_memberships(community_id, user_id, status)` (the single hottest lookup in the app, hit by every community-scoped access check). Verified with a seeded-load timing script (60 users, 5 communities, 480 memes) — all measured endpoints (feed, community feed, both leaderboards, competition standings) returned in 180-510ms, judged acceptable at this scale.
- **Accessibility**: audited and fixed the 5 named screens. Recurring gaps fixed: decorative avatar/icon elements not hidden from screen readers, meme/preview images with no accessible label, toggle/tab/radio selection state conveyed by color alone (no `accessibilityState`), leaderboard rows reading as disjoint unlabeled text fragments, and the Profile screen having no accessibility props at all. One gap flagged rather than fixed: the Creator's drag-to-position text overlay (`DraggableText.tsx`) has no non-visual alternative to dragging — added a descriptive label/role but a real fix (e.g. stepper buttons) is a UI feature addition beyond this pass's scope.
- **Regression**: full backend suite (145/145) green with all three sub-areas' changes applied together, against real Postgres + real Redis. `tsc --noEmit`, `expo lint` (0 errors, same 2 pre-existing warnings), and `expo export --platform web` (17 routes, unchanged) all clean after the accessibility edits.
- **What's not automatable and still needs a human pass**: re-running Phases 1-15's actual exit tests by hand on a live device/simulator (this timeline's convention throughout — automated tests cover backend logic, but the "does it actually work when tapped through" half has always been a separate manual gate per phase). See How to test below.

**How to test**:
This phase's manual gate is different from every prior phase's: instead of one new feature, it's re-confirming nothing regressed across all 15 previous phases, plus spot-checking the new hardening work itself.
1. **Full regression walkthrough**: starting from a clean `uvicorn`/`expo start` restart (to rule out any stale-server gotcha from earlier phases), re-run through each phase's own "How to test" section in this file, in order, on a real device/simulator — Phases 1-15. This is the bulk of the manual work; nothing here should behave differently than when each phase was originally tapped through, since Phase 16 only added indexes, rate limits, CORS config, one bug fix, and accessibility props — no behavior changes to existing flows.
2. **Rate limiting spot-check**: rapidly tap "✨ Generate a caption" more than 15 times within a minute in the Creator — expect a graceful error after the limit trips (not a crash/hang). Similarly, try logging in with a wrong password 11+ times within a minute — expect the 11th+ attempt to be rejected distinctly from a normal "wrong password" error (a 429, not a 401) — this confirms the new limiter is live, not just present in code.
3. **IDOR fix spot-check**: as user A, post a Friends-only meme. As user B (not friends with A), confirm you cannot see it in your feed (expected, unchanged). Separately (this needs API-level checking, e.g. via curl/Postman, since there's no UI path to a meme ID you can't see) confirm `POST /meme-sending/send` with that meme's ID as a non-friend-of-A sender returns 404, not 201.
4. **Accessibility spot-check**: turn on VoiceOver (iOS) or TalkBack (Android) and swipe through the Feed, Creator, Communities, Leaderboards, and Profile screens — confirm every button/tab announces a meaningful label (not "button, button"), meme images announce a description, and active tabs/selected options announce their selected state.
5. **Performance sanity check**: with your real seeded dev data (however much exists from normal testing so far), open the Feed and both Leaderboards tabs — confirm they load promptly (sub-second) with no visible lag, matching the ~200-500ms range measured in the seeded-load script.
Once all of the above are confirmed, flip Phase 16 to Done — this closes out the full 16-phase initial build.

## Phase 17 — Redis/arq infrastructure
**Goal**: replace the live-SQL/in-process stopgaps that every earlier phase deliberately deferred ("no Celery/arq infra exists yet") with real Redis caching and a real async task queue, in every spot backend/CLAUDE.md's architecture section names one — user's explicit post-MVP follow-up request, done after the initial 16-phase build rather than as its own numbered phase originally.
**Deliverables**: `arq` (chosen over Celery — Redis-native, async-first, matches this codebase's stack, lighter to operate) as the task queue; a stored `meme_scores` table + periodic arq cron recompute (replacing scoring's live-SQL-on-read); Redis-cached leaderboard reads over that stored table; challenge window-close moved from an in-process asyncio polling loop to an arq cron job; AI caption generation moved from an inline synchronous `httpx` call to an arq job (API contract kept synchronous-feeling — the request enqueues and awaits the job result, no frontend polling rewrite); Instagram metadata fetch moved from a bare `asyncio.create_task` to an arq job.
**Exit test**: full backend suite green with every change combined; the real `arq` worker process starts cleanly against the real dev Redis/Postgres and both cron jobs (scoring recompute, challenge close) actually fire on schedule and do real work (not just registered, actually executing).
**Note (2026-07-23)**: Built end to end — see `.claude/memory/redis-arq-infra.md` for full detail, plus the individual feature memory files ([[scoring-engine]], [[leaderboards]], [[challenges]], [[ai-caption]], [[instagram-companion]] — each links back to the shared infra file). Two real bugs hit and fixed along the way, both Windows/event-loop-specific and documented as gotchas in `redis-arq-infra.md`: (1) module-level Redis/arq client singletons broke across pytest's per-test event loop (`RuntimeError: Event loop is closed`) until reset between tests; (2) arq's `create_pool()` pings Redis as part of pool construction using a default 1s `conn_timeout`, which this dev environment's real ~2s first-connection latency exceeded — fixed by bumping `conn_timeout` to 10s in both `app/core/redis.py` and `app/workers/arq_worker.py`, which also fixed the standalone `arq` worker CLI (not just a test-only workaround). 145/145 backend pytest passing throughout, against real Postgres + real Redis. **Manually verified the real worker process** (not just unit-tested): started `arq app.workers.arq_worker.WorkerSettings` against the live dev DB, confirmed `close_expired_challenges` fires every 5s and `recompute_meme_scores` fires every 30s and actually recomputed real scores (8 memes, from prior seeded-load test data) — genuine end-to-end proof, not just mocked coverage. New migration `cc9508a586a7` (meme_scores table) applied to the dev DB.
**Operational note**: the `arq` worker is now a **required second process** alongside `uvicorn` — AI captions, Instagram metadata enrichment, score recomputation, and challenge window-close all silently stop working (jobs enqueue but never execute, or `POST /ai-caption/generate` will time out and 502) if only `uvicorn` is running. Start it with `arq app.workers.arq_worker.WorkerSettings` from `backend/`.

**How to test**:
1. Start three things: Postgres, Redis, and **both** `uvicorn app.main:app --port 6001` and `arq app.workers.arq_worker.WorkerSettings` (from `backend/`) — the new second process.
2. In the Creator, generate an AI caption as usual — should still work exactly as before (same button, same "Thinking…" state), confirming the enqueue-and-await-result flow behaves identically to the old inline call from the user's perspective.
3. Share an Instagram link (Feed → "Share Reel") — confirm it still fills in metadata shortly after intake, same as before.
4. Set up a short test challenge (per Phase 10's how-to-test) and confirm it still evaluates automatically after the window closes — now via the arq cron job instead of the old asyncio loop.
5. Post a few memes, react/comment on them, then check the Leaderboards tab — scores should still be correct (may take up to ~30s to reflect very recent reactions, since scoring is now cached/periodic rather than instantaneous — this is expected, not a bug).
6. As a sanity check that the worker process is actually required: stop the `arq` process (leave `uvicorn` running) and try generating an AI caption again — expect it to hang for ~15s then fail with the usual inline error message, demonstrating the job never got picked up (confirms the two-process operational note above is real, not just documentation).

---

## Daily Log
Newest entry on top. One entry per work day: what shipped, what's blocked, what's next.

### 2026-07-23, cont'd once more (Phase 17 built)
- Phase: 17 (Redis/arq infrastructure) built — user's explicit post-MVP follow-up request to stop deferring Redis/Celery-or-arq wherever it was "absolutely necessary or a better approach than live async"
- Done: Chose `arq` over Celery (Redis-native, async-first, matches this codebase's stack — confirmed with user) and covered all five spots `backend/CLAUDE.md`'s architecture section names: scoring (new `meme_scores` table + periodic arq cron recompute, replacing live-SQL-on-read), leaderboards (Redis-cached reads over that stored table, 30s TTL), challenge window-close (moved the Phase 10 in-process asyncio polling loop to an arq cron job, same guarantees), AI caption generation (moved the inline synchronous Groq call to an arq job — kept the API contract synchronous-feeling per user's confirmed choice, no frontend polling rewrite), Instagram metadata fetch (moved the Phase 15 bare `asyncio.create_task` to an arq job). New shared modules `app/core/redis.py`, `app/core/leaderboard_cache.py`, `app/workers/arq_worker.py` + `app/workers/tasks/*.py`. Hit and fixed two real Windows/event-loop-specific bugs along the way (both documented in `.claude/memory/redis-arq-infra.md`): module-level Redis/arq client singletons breaking across pytest's per-test event loop, and arq's pool-creation ping timing out under this environment's ~2s real first-connection latency against its 1s default timeout (fixed at the source, not just worked around in tests, by bumping `conn_timeout` to 10s). 145/145 backend pytest passing throughout. **Manually verified the real `arq` worker process** end-to-end (not just unit tests): started it against the live dev DB, confirmed both cron jobs fire on schedule and the scoring job genuinely recomputed real stored scores. New migration `cc9508a586a7` applied to the dev DB.
- Blocked: nothing. The `arq` worker is now a required second process alongside `uvicorn` — documented clearly in the phase entry's operational note and how-to-test, since this is a real change to how the backend must be run locally, not just new code.
- Next: human needs to do the how-to-test walkthrough above (both processes running, confirm AI captions/Instagram metadata/challenge close/leaderboards all still work exactly as before from the user's perspective, plus the "stop the arq process and confirm caption generation now fails" sanity check) before flipping Phase 17 to Done.

### 2026-07-23, cont'd yet again (Phase 16 built)
- Phase: 16 (Hardening pass) built — the final phase
- Done: Kept Phase 15 in `Testing` per user's instruction (tested once the app is installed) and moved straight to Phase 16. Worked through all four sub-areas in sequence — see the phase entry above and `.claude/memory/hardening.md` for full detail. **Security**: audited every router/service, found and fixed one real IDOR (`meme_sending.py::send_meme` let a user forward a meme they couldn't otherwise see, just by knowing its ID — fixed via the existing `get_visible_meme` visibility check), added `slowapi`+Redis rate limiting across every high-abuse-risk write endpoint (first real Redis usage in this codebase), tightened CORS from a wildcard to an explicit allow-list. **Performance**: added 7 missing indexes (most notably on `memes.created_at`, the feed's sort key, and a composite on `community_memberships` covering the app's single hottest access-control lookup) via a new migration, verified with a seeded-load script (60 users/5 communities/480 memes) showing 180-510ms latency across feed/leaderboard/competition endpoints. **Accessibility**: audited and fixed the 5 named screens (Feed, Creator, Communities, Leaderboards, Profile) — decorative avatars now hidden from screen readers, meme/preview images labeled, toggle/tab/radio selection state now exposed via `accessibilityState` instead of color alone, leaderboard rows grouped into one announcement, Profile screen's total lack of accessibility props fixed. Flagged (not fixed, out of scope for a label/role pass) one real gap: the Creator's drag-to-position text overlay has no non-visual alternative to dragging. **Regression**: full backend suite (145/145) green with everything combined, `tsc`/`expo lint`/`expo export --platform web` (17 routes) all clean.
- Blocked: nothing. The manual half of the exit test — re-tapping through Phases 1-15 by hand on a live device now that Phase 16's changes are in — is the one remaining gate, same pattern as every other phase in this timeline.
- Next: human needs to do the full manual regression walkthrough described in Phase 16's "How to test" section above (re-run every prior phase's own manual test, plus spot-check rate limiting/IDOR fix/accessibility/performance) before flipping Phase 16 — and with it, the entire 16-phase initial build — to Done.

### 2026-07-23, cont'd again (Phase 15 built)
- Phase: 15 (Instagram Companion Mode) built
- Done: Confirmed 3 scope decisions with user before building — see the phase entry above for full detail — then built end to end. Backend: `MemeContainer` + parallel `ContainerReaction`/`ContainerComment`/`ContainerVote` tables (not polymorphic, to avoid touching Phases 3/8/9/10's meme-scoped queries), stubbed pluggable oEmbed fetcher, fire-and-forget `asyncio.create_task` for metadata enrichment, `/instagram/containers/*` endpoints, `/memes/feed` response shape changed to a merged tagged-union (native memes + containers, single SQL `UNION ALL`), competitions extended with a parallel container-vote endpoint and a unioned standings/winner ranking. Challenge-submission rejection of containers required zero new code — verified by construction. 144/144 backend pytest passing (10 new), migration applied to the real dev DB (needed a manual post-autogenerate patch for reusing an existing Postgres enum type — documented as a gotcha for future phases). Frontend: new `features/instagram-companion/` (WebView feed card, comments section, share-intake modal off a new "Share Reel" button on the Feed screen), `MergedFeedList` added alongside the existing `MemeFeedList`, `VotingScreen`/`StandingRow`/`WinnerBanner` updated for the new content union. New dependency: `react-native-webview`. `tsc`/`expo lint`/`expo export --platform web` (17 routes, unchanged) all clean.
- Blocked: nothing. No human tap-through yet on Phase 15's exit test.
- Next: human needs to tap through Phase 15 — share an Instagram link, confirm it appears in the feed and its metadata fills in shortly after, confirm reactions/comments/votes work independently of native memes, confirm it's votable in the Meme of the Day standings, confirm it cannot be submitted to a challenge — before flipping Phase 15 to Done. Then start Phase 16 (Hardening pass) — the final phase.

### 2026-07-23, cont'd (Phase 13 confirmed, Phase 14 built)
- Phase: 13 confirmed Done; 14 (Sharing system) built
- Done: User gave the go-ahead to move on from Phase 13, flipped to Done. Built Phase 14 end to end — frontend-only, no backend work (this feature has no server-side surface). Confirmed scope with user first: image-only, no video export, since no video meme pipeline exists anywhere in the app (would be speculative/dead code otherwise). New deps `expo-sharing`/`expo-file-system`. `features/sharing/shareMeme.ts::shareMemeImage` downloads a meme's remote Cloudinary image to the OS cache dir then opens the native OS share sheet (`expo-sharing`'s `shareAsync`) — no custom share modal, no hardcoded target-app list, per `frontend/CLAUDE.md`. New "⤴ Share" button added directly to the existing `MemeCard.tsx` action row. `tsc`/`expo lint`/`expo export --platform web` (17 routes) all clean. No automated tests (thin native-module wrapper, no branching logic worth unit-testing — documented reasoning in `.claude/memory/sharing.md`).
- Blocked: nothing. No human tap-through yet — this needs a real Android/iOS device or simulator, not `expo start --web` (`expo-sharing` is a no-op there).
- Next: human needs to tap through Phase 14 on both iOS and Android — share a meme, confirm the native sheet shows the expected target apps plus a save-to-gallery option, confirm the saved/shared image matches the published meme exactly, confirm a network failure during download shows a graceful inline error rather than a hang — before flipping Phase 14 to Done. Then start Phase 15 (Instagram Companion Mode).

### 2026-07-23 (Phase 12 confirmed, Phase 13 built)
- Phase: 12 confirmed tested and working (per user); 13 (AI caption/joke generator) built
- Done: User confirmed Phase 12 works as desired. Built Phase 13 end to end — see the phase entry above for full detail. Chose Groq as LLM provider (user's call, key pasted and added to `backend/.env`) and `llama-3.1-8b-instant` as the model, per explicit instruction to pick whichever model burns token/rate limits slowest. Backend: `integrations/llm_client.py` (direct `httpx` call to Groq's OpenAI-compatible endpoint, 8s timeout + 1 retry on transient failures, no retry on 4xx), `services/ai_caption.py` (first-draft vs. "make it funnier" prompt branching), new `POST /ai-caption/generate` endpoint. 134/134 backend pytest passing (9 new, including direct retry/timeout/4xx tests against a mocked `httpx.AsyncClient`, no live network calls). Frontend: `services/aiCaption.ts`/`useAiCaption.ts`, a caption-generation button wired directly into the existing `CreatorScreen` (no new screen) — structurally decoupled from the Publish action so a provider failure can never block publishing. `tsc`/`expo lint`/`expo export --platform web` (17 routes) all clean.
- Blocked: nothing. No human tap-through yet on Phase 13's exit test — needs a real call against the live Groq key to confirm end-to-end (all automated tests mock the network layer).
- Next: human needs to tap through Phase 13 — generate a first-draft caption, iterate with "make it funnier", confirm publish works independent of caption generation, and confirm a deliberately-broken API key produces a graceful inline error rather than a hang/crash — before flipping Phase 13 to Done. Then start Phase 14 (Sharing system).

### 2026-07-22 (Phase 10/11 confirmed, Phase 12 built)
- Phase: 10, 11 confirmed Done; 12 (Real-time meme sending + inbox) built
- Done: User confirmed Phase 10 and 11 both tested and working — flipped both to Done. Built Phase 12 end to end — see the phase entry above for full detail. Backend: `MemeSend` model + migration, in-memory single-process `ConnectionManager` (per-user WebSocket registry, same no-Celery/arq precedent as Phase 10's worker), `/meme-sending/*` REST endpoints (send/inbox/sent/seen/react) + `WS /meme-sending/ws?token=` (JWT via query param, the one deliberate exception to Bearer-header auth since browsers can't set WS headers), live-delivery-with-inbox-fallback in `send_meme` (never assumes the recipient is connected). 125/125 backend pytest passing (7 new, including a real WebSocket integration test). Frontend: `socketSlice` (connection status), a single WebSocket connection manager singleton in `services/`, `useMemeSending.ts` (TanStack Query + socket-to-cache-invalidation sync), `SendMemeModal` (friend picker off a new button on `MemeCard`), `InboxScreen` (route `/inbox`, live status dot, auto-ack, 4 fixed emoji reactions). `tsc`/`expo export --platform web` (17 routes)/`expo lint` all clean. Hit and fixed one real bug along the way: the WS route initially opened its own DB session directly instead of going through FastAPI's `Depends(get_db_session)`, which silently bypassed the test suite's DB override — fixed by injecting the session as a WebSocket dependency (documented in `.claude/memory/meme-sending.md`). Also confirmed a scary-looking batch of 20-36 test failures across unrelated files was pure test-pollution from two `pytest` processes racing against the same real Postgres test DB concurrently, not a real regression — a clean single-process full run passed 125/125.
- Blocked: nothing. No human tap-through yet on Phase 12's exit test.
- Next: human needs to tap through Phase 12 — two friended accounts on separate clients, send a meme live while both are connected (confirm no-refresh real-time delivery + status dot), react on the receiving end and confirm the sender sees it, then send while the recipient's client is closed and confirm it lands correctly in their inbox on reconnect — before flipping Phase 12 to Done. Then start Phase 13 (AI caption/joke generator).

### 2026-07-22 (Phase 10 404 fixed, Phase 11 built)
- Phase: 10 (404 bug fix), 11 (Community challenges — community vs. community) built
- Done: User reported a 404 creating a Phase 10 challenge — root cause was the orphaned-`uvicorn`-reload gotcha (running dev server predated the challenges router). Killed and restarted cleanly, verified via `curl .../openapi.json` that routes were actually live before declaring it fixed. User also asked about 1-on-1/open "anyone can join" challenges — confirmed via question those are deferred until after all 16 phases (initial prototype) are done, not in scope now. Built Phase 11 end to end — see the phase entry above for full detail. Backend: `Challenge.opponent_community_id` (new migration), `propose_challenge`/`accept_challenge`/`decline_challenge` in `services/challenges.py`, `submit_to_challenge`/`evaluate_challenge` branch on `challenge_type` (no participant roster for `community_vs_community` — side membership and winner-award roster both derived live from `CommunityMembership`), submission eligibility requires the meme already be a community post targeting the submitter's side-community (so results flow into the existing Phase 8 community leaderboard with no new scoring path). 118/118 backend pytest passing (11 new), migration applied to the real dev DB. Frontend: `ProposeVsChallengeScreen.tsx` (owner-only, opponent picked from Discover) + `ChallengeDetailScreen.tsx` extended for the pending-proposal/accept-decline state and vs-shape side resolution. `tsc`/`expo export --platform web` (16 routes)/`expo lint` all clean. One explicit user decision before building: propose/accept flow (either owner proposes, opponent owner must accept) over immediate-active — documented in `.claude/memory/challenges.md`.
- Blocked: nothing. No human tap-through yet on either Phase 10 or 11's exit test — user is testing both together now that the 404 is resolved.
- Next: human needs to tap through Phase 10 and 11 together — Phase 10: set up a short intra-community challenge, submit from both sides, confirm late submission rejected, confirm evaluation + badge award; Phase 11: propose a challenge from one community against another, confirm only the opponent's owner can accept/decline, confirm submission requires a community-posted meme, confirm evaluation awards the whole winning community's membership and both communities' leaderboard standing reflects the result — before flipping both phases to Done. Then start Phase 12 (Real-time meme sending + inbox).

### 2026-07-22 (Phase 9 confirmed, Phase 10 built)
- Phase: 9 confirmed Done; 10 (Community challenges — intra-community team vs. team) built
- Done: User confirmed Phase 9's exit test tested and verified working — flipped to Done. Built Phase 10 end to end — see the phase entry above for full detail. Backend `Challenge`/`ChallengeSide`/`ChallengeParticipant`/`ChallengeSubmission`/`Badge` models + `services/challenges.py` (setup validation, submission window enforcement, scoring-stub-driven evaluation, points+badge award) + new `/communities/{id}/challenges/*` router + `GET /auth/me/badges`, 107/107 backend pytest passing (12 new), migration applied to the real dev DB. Stood up the repo's first background worker (`app/workers/challenges.py`, in-process asyncio polling loop via FastAPI's `lifespan`) per backend/CLAUDE.md's explicit directive that window-close must be a single consistent event — a deliberate divergence from Phase 8/9's live-SQL-on-read precedent, confirmed with user before building. Frontend `features/challenges/` (setup wizard + active/results detail screen) + a new Challenges tab on `CommunityDetailScreen`. `tsc`/`expo export --platform web` (15 routes)/`expo lint` all clean. Two explicit user decisions along the way: (1) scheduled worker over live-read for window close; (2) creator-assigned sides at setup time, no self-signup — both documented in `.claude/memory/challenges.md`.
- Blocked: nothing. No human tap-through yet on Phase 10's exit test.
- Next: human needs to tap through Phase 10 — set up a short (minutes-length) challenge with two sides, submit memes from both sides, confirm a submission attempted after window close is rejected, wait for the background worker to evaluate the challenge automatically (or check back after the window ends), confirm the correct winner is picked and the winning side's members show a badge via `GET /auth/me/badges` — before flipping Phase 10 to Done. Then start Phase 11 (Community challenges — community vs. community), which reuses this phase's lifecycle machinery scoped to two whole communities instead of two sides within one.

### 2026-07-22 (Phase 8 confirmed, Phase 9 built)
- Phase: 8 confirmed Done; 9 (Global competitions) built
- Done: User confirmed Phase 8's exit test tested and verified working — flipped to Done. Built Phase 9 end to end — see the phase entry above for full detail. Backend `Vote` model + `services/competitions.py` (period-key derivation, vote/standings/winner, all computed live — no worker, same precedent as Phase 8) + new `/competitions` router, 93/93 backend pytest passing (9 new), migration applied to the real dev DB. Frontend `features/voting/VotingScreen.tsx` (day/week/month tabs, live standings + vote buttons, previous-period winner banner), first use of `date-fns` in this codebase. `tsc`/`expo export --platform web` (13 routes)/`expo lint` all clean. Two explicit user decisions along the way: (1) period-close handled by live SQL computation rather than standing up a new Celery/arq scheduler; (2) the `Vote` unique constraint follows backend/CLAUDE.md's literal `(user_id, meme_id, period)` wording (one vote per meme per period) over `Project_Requirements.md` §9's looser "one vote per period" prose — both documented in `.claude/memory/voting-system.md`.
- Blocked: nothing. No human tap-through yet on Phase 9's exit test.
- Next: human needs to tap through Phase 9 — cast a first vote on a public meme (succeeds), attempt a second vote on the *same* meme in the same period (rejected, 409), confirm a vote on a non-public meme is rejected (400), confirm the winner endpoint rejects a still-open period and correctly surfaces the top-voted meme for a closed one — before flipping Phase 9 to Done. Then start Phase 10 (Community challenges — intra-community team vs. team).

### 2026-07-21 (Phase 8)
- Phase: 8 (Scoring stub + leaderboards)
- Done: Built Phase 8 end to end — see the phase entry above for full detail. Backend `services/scoring.py` (live-SQL placeholder score) + `services/leaderboards.py` (individual/global-community/internal-community, all read-only, offset-paginated), new router + a community-scoped route added to `routers/communities.py` matching established convention. 84/84 backend pytest passing (8 new, covering ranking correctness and all three access-control cases). Frontend `features/leaderboards/` (new tab screen) + a third tab on `CommunityDetailScreen`. `tsc`/`expo export --platform web` (12 routes)/`expo lint` all clean. Chose to recompute scores live in SQL rather than stand up Celery/arq infra this phase (user's explicit call when asked) — documented as an interim decision in `.claude/memory/scoring-engine.md`, revisit once the real scoring engine's inputs stop being cheap live aggregations.
- Blocked: nothing. No human tap-through yet on Phase 8's exit test.
- Next: human needs to tap through Phase 8 — seed known reactions/comments across a few users/communities and confirm all three leaderboards rank correctly, confirm a non-member is rejected from an internal community leaderboard, confirm no leaderboard endpoint accepts a write — before flipping Phase 8 to Done. Then start Phase 9 (Global competitions — Meme of the Day/Week/Month).

### 2026-07-21 (Phase 7 confirmed, Phase 8 starting)
- Phase: 7 confirmed Done; 8 (Scoring stub + leaderboards) starting
- Done: User confirmed Phase 7's exit test tested perfectly fine (open-community post appears in both community + public feed with badge, invite-only stays community-only, non-member posting rejected server-side). Exit test green, phase flipped to Done.
- Blocked: nothing.
- Next: build Phase 8 — `services/scoring.py` stub (likes + comments, clearly labeled placeholder per Project_Requirements §7 — not the final rules engine), background recompute worker, `MemeScore`-style aggregate storage, individual leaderboard, global community leaderboard, internal per-community leaderboard (member-gated), RN Leaderboards tab + community-scoped leaderboard tab. All three surfaces read-only — no write/submission endpoint.

### 2026-07-21 (Phase 7, redesigned mid-phase)
- Phase: 7 (Community posting)
- Done: Built Phase 7 twice — see the phase entry above for the full "design history." First pass shipped a generic-creator multi-select (Public/Friends/communities) matching the original `Project_Requirements.md` §4; user redirected the design (community posting must be its own flow, entered only from inside a community, audience fully automatic from that community's privacy, no cross-posting to multiple communities). Rewrote `Project_Requirements.md` §3/§4/§17 to match, then reworked the implementation: backend `POST /communities/{id}/memes` (new, member-gated, server-derives audience from `community.privacy`) replaces the interim `community_ids` field on `POST /memes` (reverted to Public/Friends-only); `MemeOut.community_ids: uuid[]` replaced with `community: {id, name} | null`; `require_active_membership` now returns the `Community` row. 76/76 backend pytest passing (rewritten community tests). Frontend `CreatorScreen` now branches on an optional `communityId` route param instead of showing a communities multi-select; entry point moved to a "+ Post" button inside `CommunityDetailScreen`; `MemeCard` shows the community badge. `tsc`/`expo export --platform web` (11 routes)/`expo lint` all clean.
- Blocked: nothing. No human tap-through yet on Phase 7's exit test.
- Next: human needs to tap through Phase 7 — post in an open community and confirm it appears in both that community's feed and the public feed with the community visibly attached; post in an invite-only community and confirm it stays community-only; confirm posting to a community you're not a member of is rejected server-side — before flipping Phase 7 to Done. Then start Phase 8 (Scoring stub + leaderboards).

### 2026-07-21 (Phase 6 confirmed, Phase 7 starting)
- Phase: 6 confirmed Done; 7 (Multi-audience posting + community) starting
- Done: User tapped through Phase 6's exit test on live dev servers — member-uploaded community template correctly excluded from a non-member's global list and rejected (403) on direct API access, and correctly visible/applicable in the creator for a member. Exit test green, phase flipped to Done.
- Blocked: nothing.
- Next: build Phase 7 — extend the Phase 3 audience system to include one-or-more communities: creator audience selector's Community option wired live (multi-select from the user's own communities), community feed endpoint/screen, backend audience validation (can't post to a community you're not a member of).

### 2026-07-21 (Phase 6)
- Phase: 6 (Community-private templates)
- Done: Added nullable `template.community_id` + migration, `require_active_membership` shared helper on `services/communities.py`, optional `community_id` on `POST /templates` (member-gated), new member-gated `GET /communities/{id}/templates` browse endpoint (no open-community exception, unlike the Phase 5 member-list rule), global list now excludes community templates. 67/67 backend pytest passing (6 new). Frontend: `TemplatePickerModal` now has a Global/per-community scope-tab row backed by a new `useCommunityTemplates` hook and a shared `TemplateGrid` presentational component. `tsc`/`expo export --platform web`/`expo lint` all clean.
- Blocked: nothing. No human tap-through yet on Phase 6's exit test.
- Next: human needs to tap through Phase 6 — a member uploads a community template, confirm a non-member's global template list doesn't include it and a direct API request for the community's template list as a non-member is rejected (403), confirm a member sees and can apply it in the creator — before flipping Phase 6 to Done. Then start Phase 7 (Multi-audience posting + community).

### 2026-07-21 (Phase 5 confirmed, Phase 6 starting)
- Phase: 5 confirmed Done; 6 (Community-private templates) starting
- Done: User tapped through all three parts of Phase 5's exit test across two sessions (open-community instant join, invite-only pending-then-approve, non-member 403 on an invite-only community's members). Fixed a real Expo Router routing bug (`communities/index.tsx` colliding with the sibling `[id].tsx` dynamic route — moved the list screen to a flat `communities.tsx`) and quieted a noisy `console.error` for gracefully-handled API errors. Exit test green, phase flipped to Done.
- Blocked: nothing.
- Next: build Phase 6 — `template.community_id`, community template upload/browse endpoints (member-only regardless of community privacy setting — no open-community carve-out here, unlike the members-list rule), RN Templates split (global vs. community) in the creator's template picker.

### 2026-07-21 (Phase 5)
- Phase: 5 (Communities core)
- Done: Built out Phase 5 end to end — backend `Community`/`CommunityMembership` models (owner/member roles, open/invite-only privacy), `/communities` create/discover/mine/get/join/leave/members/join-requests(list/approve/reject) endpoints, migration applied to the real dev DB, 61/61 backend pytest passing (15 new). Frontend Communities tab (My Communities/Discover), Create Community screen, Community Detail screen (join/leave/pending states, owner join-request approval, members list) — first folder-based nested route group in the app, hit and worked around an Expo Router typed-routes quirk around bare vs. index folder hrefs. `tsc`/`expo export --platform web`(11 routes)/`expo lint` all clean. Hit the same orphaned-`uvicorn`-worker stale-server issue as Phase 4 (twice) — restarted cleanly each time, same root cause as documented in `.claude/memory/meme-creator.md`.
- Blocked: nothing blocking. No human tap-through yet on Phase 5's exit test.
- Next: human needs to tap through Phase 5 on a simulator/device — create an open community and confirm a second user can join with no approval step, create an invite-only community and confirm a join request requires owner approval, confirm a non-member gets 403 (not just hidden UI) on member-only reads for an invite-only community — before flipping Phase 5 to Done. Then start Phase 6 (Community-private templates).

### 2026-07-21 (Phase 4 confirmed, Phase 5 starting)
- Phase: 4 confirmed Done; 5 (Communities core) starting
- Done: Fixed a stale-`uvicorn`-worker bug blocking manual testing (orphaned reload child process serving pre-Phase-4 routes — see gotcha in `.claude/memory/meme-creator.md`) and seeded a real global template from a user-supplied image via the actual `/templates` endpoint. User then tapped through Phase 4 on live dev servers and confirmed it works. Exit test green, phase flipped to Done.
- Blocked: nothing.
- Next: build Phase 5 — `Community`/`community_membership` models, owner-set privacy (open/invite-only), create/join/leave/invite-approve endpoints, RN Communities tab (My Communities / Discover) + Community Detail shell (Members tab only for now).

### 2026-07-21 (Phase 4)
- Phase: 4 (Meme creator + global templates)
- Done: Phase 3 confirmed Done (user tapped through Public/Friends visibility + reactions/comments on live dev servers). Started and built out Phase 4: backend `Template` model + `/templates` create/list endpoints (global scope only), extracting two shared helpers along the way (`services/media.py` for image-upload validation, `core/pagination.py` for keyset cursors) since templates is the 2nd consumer of both — 46/46 backend pytest passing. Frontend `features/creator/CreatorScreen` (own-image or template pick → draggable top/bottom text overlay → view-shot preview capture → caption/audience → publish), replacing the old `NewPostScreen` at `/new-post`; added `react-native-view-shot` dependency and a `GestureHandlerRootView` wrapper for gesture-handler's first real usage in this codebase. `tsc`/`expo export --platform web`/`expo lint` all clean.
- Blocked: nothing blocking. No human tap-through yet on Phase 4's exit test (pick a template, add top/bottom text, confirm preview matches published post, confirm it appears in the feed).
- Next: human needs to tap through Phase 4 on a simulator/device — upload own image and separately pick a template, drag top/bottom text, preview, publish, confirm it shows correctly in the Phase 3 feed — before flipping Phase 4 to Done. Then start Phase 5 (Communities core).

### 2026-07-21 (Phase 3)
- Phase: 3 (Public feed)
- Done: Picked Cloudinary for media storage (user decision) and wired credentials into `backend/.env`. Built `Meme`/`PostAudience`/`Reaction`/`Comment` models + migration (applied to real dev Postgres), `/memes` endpoints (create via multipart upload, keyset-paginated feed, reactions, comments) with server-enforced audience visibility reusing Phase 2's friendship model. 38/38 backend pytest passing (Cloudinary mocked in tests; real upload path separately smoke-tested against live Cloudinary — confirmed a real, publicly-reachable image URL). Frontend: `FeedScreen` (infinite scroll, pull-to-refresh), `NewPostScreen` (image picker + caption + Public/Friends audience toggle — added `expo-image-picker` dependency), inline comments on feed cards. `tsc`/`expo export --platform web` clean (8 routes).
- Blocked: nothing blocking. Two real bugs hit and fixed mid-build (both documented in `.claude/memory/meme-feed.md`): an async SQLAlchemy `db.get()`-vs-`db.refresh()` relationship-loading gotcha, and a FastAPI `Form(PydanticModel)` multipart gotcha. Also hit and fixed a `uvicorn --reload` issue where watching `venv/` caused the reloader to wedge after `pip install` — now scoped to `--reload-dir app`.
- Next: human tap-through on a simulator/device for Phase 3's exit test (post Public/Friends-only from two accounts, confirm visibility + reactions/comments round-trip), then start Phase 4 (Meme creator + global templates).

### 2026-07-21 (cont'd)
- Phase: 1 & 2 confirmed Done; 3 (Public feed) started
- Done: Started `uvicorn` (port 6001) and `expo start --web` (port 8081) as live dev servers; user manually tapped through register → logout → login → profile (Phase 1) and friend request send → accept → mutual friends-list visibility (Phase 2). Both confirmed working — both phases flipped to `Done`.
- Blocked: nothing.
- Next: build Phase 3 — `Meme`/`post_audience` models, image upload endpoint, infinite-scroll feed filtered by Public/Friends audience, reactions + comments, RN feed screen.

### 2026-07-21
- Phase: 2 (Friends)
- Done: Backend — `Friendship` model (`backend/app/models/friendship.py`) with a DB-level direction-independent unique constraint (`LEAST`/`GREATEST`-computed columns) so a request can't exist twice in either direction; `/friends` endpoints (send/accept/remove/list/list-requests) in `routers/friends.py` + `services/friends.py`; 6 new domain exceptions; Alembic migration generated and applied to the real dev Postgres DB; 16 new pytest (22/22 total) passing against real Postgres. Frontend — `friends.ts`/`useFriends.ts` service layer, `FriendsScreen` (single `FlatList`, add-friend form + incoming requests in the header), `/friends` route reachable via a button on the profile screen; promoted `AuthTextField` → `components/TextField.tsx` (2nd consumer) and updated Login/RegisterScreen. `tsc --noEmit` and `expo export --platform web` both clean (6 static routes including `/friends`).
- Blocked: nothing blocking. **Process note**: started this phase without Phase 1's manual device tap-through being confirmed — user explicitly chose to skip that gate rather than block on it. Both phases now carry the same open item (see below).
- Next: human needs to tap through both flows on a simulator/device/`expo start` — Phase 1 (register → logout → login → profile) and Phase 2 (A sends request to B → B accepts → both see each other in Friends → C doesn't) — before flipping either row to `Done`. Then start Phase 3 (Public feed).

### 2026-07-20
- Phase: 1 (Auth & profile)
- Done: Backend — `User` model, JWT auth (`bcrypt` + `python-jose`), `/auth/register|login|me`, Alembic migration applied to a real local Postgres (`meme_platform` dev DB), 9/9 pytest passing against a dedicated `meme_platform_test` DB. Frontend — NativeWind/Redux Toolkit/TanStack Query/React Hook Form+Zod wired in; login/register/session screens; `expo-secure-store` session persistence with a bootstrap-on-launch flow; removed the leftover Expo demo screens/components. `tsc --noEmit` and `expo export --platform web` both clean. Set up `.claude/memory/` (README + `auth-profile.md`).
- Blocked: nothing blocking. Local Postgres found on `127.0.0.1:5432` (password provided by user), separate `meme_platform`/`meme_platform_test` DBs created so this project doesn't share state with the sibling `Lead_Generator` project.
- Next: human needs to actually tap through the app (register → logout → login → see profile) on a simulator/device/`npm run android`/`npm run web` to close out Phase 1's exit test, then start Phase 2 (Friends).

<!--
### 2026-XX-XX
- Phase: #
- Done:
- Blocked:
- Next:
-->
