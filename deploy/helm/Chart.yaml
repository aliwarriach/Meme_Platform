apiVersion: v2
name: meme-platform
description: >
  api / realtime / worker Deployments from the single A6 image
  (Roadmap_Scaling.md C2). One chart, different values files —
  values-dev.yaml for local/dev scale, values-loadtest.yaml for load testing.
type: application
version: 0.1.0
appVersion: "1.0"
