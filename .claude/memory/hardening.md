# hardening (Phase 16, + 2026-08-19 security audit remediation)

## 2026-08-19 security audit remediation
The `security-auditor` agent ran a full OWASP Top 10:2025 / ASVS 5.0 pass, writing `.claude/memory/SecurityIssues.md` (what's implemented and wrong) and `.claude/memory/SecurityFeatures.md` (what's missing entirely) — **read those two files first** for the full classification/findings; this section only summarizes what got fixed and where. User explicitly deferred **M-2/M-3** (per-account/global rate limits and login lockout) — do not add those without asking, they were declined on purpose (AI features are meant to stay unlimited for now).

**Fixed, by finding ID:**
- **H-1** (every user's email leaked platform-wide) — `PublicUserOut`/`PublicUserResponse` split, see `[[auth-profile]]`.
- **H-2** (cleartext HTTP shipped in Android builds) — `frontend/app.json` → `frontend/app.config.js` (dynamic config), `usesCleartextTraffic` now scoped out of `EAS_BUILD_PROFILE=production` builds only; `frontend/src/constants/config.ts::resolveApiBaseUrl` throws instead of falling back to `http://` when `!__DEV__` and `EXPO_PUBLIC_API_URL` is unset. **Still needed**: set a real `https://` `EXPO_PUBLIC_API_URL` in `eas.json`'s `production` profile once a real backend deployment exists — there isn't one yet, so this is currently enforced by the runtime throw, not a build-time value.
- **M-1** (session JWT in the WS URL) — `POST /meme-sending/ws-ticket` mints a 30s single-use ticket (Redis, via `get_arq_pool()` — it's a full `redis.asyncio.Redis`, reused rather than standing up a second client); `GET /meme-sending/ws` now takes `?ticket=` instead of `?token=`. Logic lives in `services/meme_sending.py::create_ws_ticket`/`redeem_ws_ticket`, not the router (kept thin) — this also matters for tests, see Gotchas below. `frontend/src/services/memeSendingSocket.ts` fetches the ticket via `api.post` before opening the socket.
- **M-4** (prompt injection into the caption LLM) — untrusted `context`/`current_caption` fenced in a `<user_data>` block in `services/ai_caption.py::_build_prompt`, system prompt (`integrations/llm_client.py`) told to treat that block as data never instructions; response also hard-truncated to 100 chars server-side rather than trusting the model to honor the limit.
- **M-5** (unpinned backend deps, no lockfile) — `requirements/{base,prod,dev}.txt` now pin exact versions; `requirements/{base,prod,dev}.lock` are hash-verified lockfiles generated via `pip-tools` (`python -m piptools compile --generate-hashes`, added to `dev.txt`). Install with `pip install -r requirements/dev.lock --require-hashes` (README updated).
- **M-6** (platform account squatting) — `User.is_platform_account` (new column, migration `d069c6a9dd24`); `challenges.py::_get_or_create_platform_user` resolves by that flag now, not by the `memeversehq` username. `services/auth.py::RESERVED_USERNAMES` also blocks registering it (and other system-ish names) in the first place.
- **M-7** (logout didn't revoke server-side) — `authSlice.ts::signOut` now calls `POST /auth/logout` first. See `[[auth-profile]]`.
- **L-1** (registration = email-existence oracle) — generic conflict message, see `[[auth-profile]]`.
- **L-2** (push token silently stealable) — `services/notifications.py::register_push_token`: reassigning a token to a different user is a no-op unless the existing binding has gone stale (`PUSH_TOKEN_REASSIGN_STALE_AFTER`, 30 days) — a real reinstall/re-login re-registers the token in that window anyway.
- **L-3** (interactive docs public) — `/docs`/`/redoc`/`/openapi.json` disabled when `ENVIRONMENT=production` (new `Settings.environment`/`is_production`, `main.py`).
- **L-4** (WebView could navigate anywhere) — `_validate_instagram_url` (`services/instagram.py`) now requires `https://` (was `http?://`); `ContainerCard.tsx`/`WebContainerCard.tsx` add `originWhitelist` + `onShouldStartLoadWithRequest` pinned to instagram.com.
- **L-5** (ngrok URL committed in `eas.json`) — removed; `preview` profile now uses `"environment": "preview"` — **requires a one-time manual step**: run `eas env:create --environment preview --name EXPO_PUBLIC_API_URL --value <tunnel-url>` (needs the user's EAS login, can't be scripted here).
- **L-7** (CORS defaults to localhost) — see `[[auth-profile]]`'s CORS note.
- **L-8** (unthrottled friend requests) — `POST /friends/requests` now `20/minute`.
- **I-1** (no security headers) — `main.py::_security_headers` middleware (nosniff/frame-options/referrer-policy/HSTS); `frontend/public/_headers` adds a CSP to the static web export (Netlify/Cloudflare Pages convention — **not yet verified against a real `expo export --platform web` build**, tighten `connect-src`/`frame-src` once a real backend origin is fixed).
- **I-2** (no root `.gitignore`) — added.

**Declined by user request:** M-2, M-3 (do not add without asking).

## Gotchas from this pass
- **`get_arq_pool()` is a module-level singleton bound to the event loop that first called it.** `services/meme_sending.py`'s new ticket store calls it directly (like every other service already does for arq jobs) rather than opening a second Redis client — but this means a router must **never** call `get_arq_pool()` itself (breaks `tests/conftest.py`'s `use_fake_arq_pool` fixture, which patches `get_arq_pool` per-service-module, not globally) and any new caller needs its own `monkeypatch.setattr(<module>, "get_arq_pool", get_fake_arq_pool)` line added to that fixture. `tests/fake_arq.py::FakeArqPool` now also fakes plain `set`/`getdel` (not just `enqueue_job`), backed by one **shared** module-level instance (`get_fake_arq_pool` used to return a fresh instance per call, which broke any code that sets a key in one call and reads it in another — the real pool is shared, so the fake has to be too).
- `tests/conftest.py` also gained `_reset_connection_manager` (clears `app.websockets.connection_manager`'s `_connections` dict between tests) — same class of cross-event-loop singleton leak as `_reset_leaderboard_cache`, just never triggered before because the WS ticket tests used to fail before ever reaching a live connection.

## 2026-08-19 SecurityFeatures follow-up (EXPECTED + RECOMMENDED bands approved; F-9 explicitly declined by user)
- **F-3 (partial — `is_active` only, no self-service/admin deactivation endpoint yet)**: `User.is_active` (migration `027492eb1039`, default `true`). Checked in `core/deps.py::get_current_user` (same rejection as an invalid token), `services/auth.py::authenticate_user` (same rejection as a wrong password — doesn't confirm the account is disabled either), and the WS ticket-redemption path in `routers/meme_sending.py`. Nothing sets this to `false` yet — there's no staff/admin role (still absent) and no self-deactivation endpoint (that's the rest of F-3, not done in this pass) — so it's ops-only for now, e.g. a manual `UPDATE users SET is_active = false`.
- **F-5 (item 1 only — blocking)**: see `[[blocks]]` for the full writeup. New `blocks` table + `services/blocks.py`, wired into `meme_visibility_clause`, `are_friends`, and `send_friend_request` exactly as the audit scoped it. Items 2–5 (report capture, suspend/ban, a moderation surface, upload scanning) are **not built** — still open gaps.
- **F-6 (security event logging)**: new `core/logging.py` — stdlib `logging` + a hand-rolled JSON formatter (chose "stdout only, no new dependency" over a Sentry integration when asked). `configure_logging()` runs once at import time in `main.py`. A `request_id` (`contextvars`) is stamped by the outermost middleware (`main.py::_request_id_and_unhandled_exceptions`) and returned as `X-Request-ID`; every `log_security_event(...)` call picks it up automatically. Events emitted: `auth.register_success`/`auth.login_success`/`auth.logout` (explicit calls in `services/auth.py`), `security.forbidden` for **every** 401/403 `DomainError` (one choke point — `core/exceptions.py`'s existing handler, not instrumented per-service), `security.rate_limited` (the `RateLimitExceeded` handler in `main.py`), `security.unhandled_exception` (same outermost middleware, logs then re-raises so the response is unchanged). **Never logs the JWT, password, or DM/message body** — only ids/usernames/event metadata. No log shipping yet (F-14, MATURITY band, not in scope) — logs go to stdout only.
- **F-1 (email verification)**: OTP-based, per user decision — Gmail API + OAuth2 refresh token (not SMTP/App Password, and not a third-party transactional-email provider like Resend/Postmark). `User.email_verified_at` (nullable, migration `d34ba3d4f7db`). `integrations/gmail_client.py` does the OAuth2 refresh **and** the send as raw `httpx` calls — no `google-auth`/`google-api-python-client` SDK (tried `google-auth` first; its sync transport requires the `requests` package, a poor fit here, so it was removed again before ever landing in a lockfile). Needs `GOOGLE_OAUTH_CLIENT_ID`/`_SECRET`/`_REFRESH_TOKEN` + `GMAIL_SENDER_EMAIL` in `.env` — **all optional**, app starts fine without them, `/auth/email/verify/request` just fails with `EmailSendError` until configured. **One-time setup the user still needs to do**: Google Cloud Console project → enable Gmail API → OAuth 2.0 Client ID (Desktop app) → run a one-time consent flow for the `gmail.send` scope on the sending account → paste the resulting refresh token into `.env`. Not something this session could do (needs the user's Google login).
  - OTP mechanics (`services/email_verification.py`): 6-digit code, **SHA-256 hashed** at rest in Redis (`email-otp:{user_id}`, 10 min TTL), separate attempts counter (`email-otp-attempts:{user_id}`, max 5 before lockout forcing a fresh request). `POST /auth/email/verify/request` (3/hour), `POST /auth/email/verify/confirm` (10/hour, body `{code}`).
  - **Gating, exactly the 4 surfaces the audit named** (`core/deps.py::CurrentVerifiedUser`, 403 `EmailNotVerifiedError` if `email_verified_at is None`): `POST /ai-caption/generate`, `POST /memes/{id}/votes`, `POST /instagram/containers/{id}/votes`, `POST /communities`. **Messaging is gated at the service layer instead of the router** — `services/messaging.py::_get_or_create_conversation` takes `current_user` now (not just `viewer_id`) and only requires verification on the branch that actually creates a *new* conversation, never on reusing an existing one; this covers both `POST /messaging/conversations` and the legacy `/meme-sending/send` shim for free, since both funnel through that one function. Login/registration are never gated.
  - Frontend: `services/emailVerification.ts` + `useEmailVerification.ts` + `features/auth/EmailVerificationBanner.tsx`, shown on the **native** profile screen (`SessionScreen.tsx`) when `user.emailVerifiedAt` is null. **Not built on the desktop-web profile screen** (`SessionScreen.web.tsx`) — it uses a completely different inline-`StyleSheet`/`PROFILE_WEB_*` design system than the native NativeWind one, and reusing the native component as-is would look inconsistent there. Flagged, not silently skipped — build a matching web version before relying on this on desktop web.
  - Tests must never rely on real Gmail: `tests/conftest.py::mock_gmail_send` patches `workers.tasks.email_verification.send_email`. `tests/conftest.py::create_user` now auto-verifies every user it creates (direct DB write via a new `mark_email_verified()` helper — bypasses the OTP flow entirely) so the other ~230 existing tests didn't need touching; a few raw-`TestClient`-register websocket tests (`test_meme_sending.py`, `test_messaging.py`) call `mark_email_verified()` themselves since they don't go through `create_user`. Dedicated coverage in `tests/test_email_verification.py` (9 tests) uses **unverified** users on purpose.
- **F-4 (content deletion/editing)**: **soft delete**, per user decision. New `db/base.py::SoftDeleteMixin` (`deleted_at`, nullable) — applied to `Meme` and `Comment` only in this pass (**not** `MemeContainer`/`ContainerComment` — flagged as a follow-up, not done). `DELETE /memes/{id}` and `DELETE /memes/{id}/comments/{id}`, author-only (403 otherwise), 404 on anything already-gone (never confirms whether it existed for someone else). `meme_visibility_clause` and the comment-list query both exclude `deleted_at IS NOT NULL` rows; `get_meme_out_for_viewer` (messaging's embedded-meme renderer) treats a deleted meme as "not found" — degrades to the null-meme placeholder `MessageOut.meme` already tolerated *before* this feature existed (that comment was written anticipating this). Cloudinary cleanup (`integrations/cloudinary_client.py::delete_image`, wrapped by `services/media.py::delete_uploaded_image`) is **best-effort and never raises** — a failed Cloudinary delete never blocks or un-does the soft-delete, it only logs.
  - Also added `PATCH /auth/me` (bio + avatar) — `Annotated[str | None, Form()]` **cannot distinguish an empty submitted string from an omitted field** (confirmed empirically: Starlette collapses both to `None`), so clearing the bio needs its own explicit `clear_bio: bool` form field rather than `bio=""` — don't reintroduce that bug if this endpoint is touched again. `User.avatar_public_id` (new column) tracks the avatar's Cloudinary asset so a *replaced* avatar's old one actually gets cleaned up, symmetric with `Meme.image_public_id`.
  - Tests need `mock_media_delete` (new `conftest.py` fixture, same "patch where imported" pattern as `mock_media_upload` — `delete_uploaded_image` is patched on `memes_service` and `users_service` individually, not on `media_service`, since each imports the name directly into its own namespace).
  - **Not done**: community-owner removing a member's post from their own community's feed (the audit's stretch scope beyond plain author-deletion) — still a gap.
- **F-11 (CI security gate)**: `.github/workflows/ci.yml` (repo had **zero** CI before this) — backend job (Postgres 16 + Redis 7 service containers, hash-verified install from `dev.lock`, `ruff check`, `alembic upgrade head`, `pytest`, advisory `pip-audit`), frontend job (`tsc --noEmit`, `expo lint`, advisory `npm audit --audit-level=high`), and a `gitleaks` secret-scan job. Both dependency-audit steps are **advisory (`|| true`), not blocking** — `pip-audit` because M-5's pinned versions were confirmed current at audit time and a future CVE shouldn't fail every PR before someone's triaged it, `npm audit` because L-6's residual findings are a deliberately accepted gap (build/dev-time tooling only, deferred to the next Expo SDK bump) and would otherwise make CI permanently red. `.github/dependabot.yml` covers pip, npm, and github-actions ecosystems, weekly. **Not yet verified against a real GitHub Actions run** — validated the YAML parses, but the workflow itself hasn't executed on GitHub yet; check the Actions tab after the next push.
- **F-2 (password reset)**: OTP-based, per user decision — same mechanics as F-1 (`services/password_reset.py`, duplicated rather than shared: 2 call sites, and the two flows have genuinely different rules — reset must never confirm whether an email exists, verification always knows exactly who it's for). `POST /auth/password-reset/request` (no auth, body `{email}`, **always 204** regardless of whether the email is registered, active, or the platform's seeded account — SecurityIssues.md L-1's oracle rule extended here), `POST /auth/password-reset/confirm` (no auth, body `{email, code, new_password}`, `400`/`429` on bad/exhausted code). Confirming **bumps `token_version`**, so a successful reset silently kills every existing session — matters if the reset was prompted by a credential-stuffing takeover (SecurityIssues.md M-3's scenario). Also added authenticated `POST /auth/change-password` (`{current_password, new_password}`, `401` on wrong current password) for a user who still has their password and just wants to rotate it — also bumps `token_version`.
  - Worker job `send_password_reset_otp_job` (`workers/tasks/password_reset.py`), registered in `arq_worker.py`. Same `mock_gmail_send` conftest fixture now patches both `email_verification` and `password_reset` worker-task modules.
  - **No frontend UI built** for this one (no "forgot password?" link/screen yet) — backend-only in this pass, unlike F-1 which got a banner. Flagged, not an oversight.
- **F-8 (secrets hardening, partial)**: `core/config.py::Settings` now has a Pydantic `field_validator` on `jwt_secret` that **fails app startup unconditionally** (not gated on `ENVIRONMENT=production`) if it's the literal `changeme` or under 32 characters — length is used as an entropy proxy per the audit's own reasoning (real entropy needs the generation process, not just the output). Verified against the actual dev `.env` first (64 chars, passes) before landing this, specifically so it wouldn't silently break the running dev setup. `.env.example`'s `JWT_SECRET` placeholder changed from `changeme` to the still-short `REPLACE_ME` — deliberately kept short enough to itself trip the new check if someone copy-pastes it verbatim, rather than a longer placeholder that might accidentally clear the length bar. **Not done**: Cloudinary key scoping (upload-only), a provider-side Groq spend cap, a written key-rotation runbook, and moving runtime secrets into an actual managed secret store/KMS — all deployment-time actions with no deployment target to configure them against yet.
- **F-13 (age gate, code only — no privacy-policy legal text, per user decision)**: `User.date_of_birth` (nullable — existing pre-migration accounts have none; **required** in `RegisterRequest` for every new signup going forward). `services/auth.py::register_user` computes age from DOB and rejects (`400`, `UnderMinimumAgeError`) anyone under 13 **before creating any row** — nothing about a rejected attempt is persisted. Frontend mirrors the same 13+ check client-side in `features/auth/schemas.ts::registerSchema` (inline error before the request even fires) — the server-side check is what actually enforces it; the client one is just faster feedback. New `dateOfBirth` field (plain `YYYY-MM-DD` text input, not a native date-picker component — didn't want to add a new native dependency this session without being able to test it on-device) on `RegisterScreen.tsx`, the only registration screen (no separate web variant exists). **No privacy-policy/terms text or processor-disclosure screen was written** — user explicitly scoped this to code only; that half of F-13 is still a real gap before any public launch.
- **Migration note for F-1/F-2/F-8/F-13 as a batch**: every new `User` column in this second pass (`date_of_birth`) — like every column in the first pass — triggered Alembic autogenerate to also propose `DROP TABLE imgflip_templates` (pre-existing DB drift unrelated to any of this work, first noticed in the M-6 migration). Stripped out of every migration file by hand each time; still unresolved as its own issue. If autogenerate is run again for anything touching `users`, expect this to reappear — keep stripping it, or actually resolve the drift once, in its own reviewed migration.
- **F-7 (MFA, via "Sign in with Google" — per user decision, full sign-up + login, not login-only)**: `User.google_sub` (new column, unique, nullable — `[[hardening]]`'s per-User-column migration-drift note applies here too). `services/google_auth.py` verifies the client-supplied Google ID token against Google's own `oauth2.googleapis.com/tokeninfo` endpoint via raw `httpx` (no `google-auth`/JWKS-caching SDK — same "no heavy SDK" call as F-1's Gmail integration, and the same lesson from that attempt: keep it to a REST call, don't reach for a Google auth library in this codebase). Checks `aud` against `settings.google_signin_client_ids` (comma-separated — Expo issues a different `aud` per platform) and requires `email_verified: true` from Google itself.
  - `POST /auth/google` (body `{id_token}`) returns **one of two shapes, discriminated by JSON content, not by a single status code alone in the client's apisauce wrapper** (`isPendingRegistration()` on the frontend checks for a `pending_token` key): `200 TokenResponse` if `google_sub` already matches a user, or if the verified email matches an existing password account (which also **links** it — sets `google_sub` and marks `email_verified_at` if it was still null, since Google already proved the email); `202 GooglePendingRegistrationOut {pending_token, email}` if no account exists at all. `POST /auth/google/complete` (body `{pending_token, username, date_of_birth}`) actually creates the account — same age-gate (F-13) and reserved-username (M-6) checks as normal registration, `hashed_password` is a random discarded string (this account only ever authenticates via Google), `email_verified_at` set immediately. Pending tickets live in Redis (`google-pending-reg:{token}`, 10 min TTL, single-use via `getdel`) — same shape as every other OTP/ticket store in this codebase (M-1's WS tickets, F-1/F-2's OTPs).
  - **Two separate Google OAuth credential sets exist in this codebase now, don't confuse them**: `google_oauth_client_id`/`_secret`/`_refresh_token` (F-1, a "Desktop app" credential, server-side only, for sending Gmail) vs. `google_signin_client_ids` (F-7, `EXPO_PUBLIC_GOOGLE_{IOS,ANDROID,WEB}_CLIENT_ID` on the frontend, iOS/Android/Web credential types, used client-side by `expo-auth-session`). Same Google Cloud Console project, different OAuth clients within it.
  - Frontend: new dependency `expo-auth-session` (`npx expo install`, SDK-resolved to `~57.0.7`), `services/googleAuth.ts` + `useGoogleAuth.ts` + `features/auth/GoogleSignInFlow.tsx` (one component handles both the login-only and complete-registration cases, dropped into both `LoginScreen.tsx` and `RegisterScreen.tsx`). Uses `Google.useIdTokenAuthRequest` specifically (confirmed against the installed package's own `.d.ts`, not assumed from memory — `useAuthRequest` alone returns an auth *code*, not an ID token; the Id-Token-specific hook is what this flow needs).
  - **2026-08-20: Web client ID configured** — `backend/.env`'s `GOOGLE_SIGNIN_CLIENT_IDS` and `frontend/.env`'s `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` both set to a real "Web application" OAuth client (`...antbtf.apps.googleusercontent.com`). Google also issued a client **secret** for this Web client type, but it's deliberately **not stored anywhere** — `useIdTokenAuthRequest` is a client-side ID-token flow with no server-side code exchange, so no secret is needed, and an `EXPO_PUBLIC_` var ships in the public bundle (never a safe place for one anyway). iOS/Android client IDs (`EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`/`_ANDROID_CLIENT_ID`) are still unset — see Android setup note below.
  - **2026-08-20: Android client ID configured** — `frontend/.env`'s `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID` and `backend/.env`'s `GOOGLE_SIGNIN_CLIENT_IDS` (now comma-separated Web + Android) both set to a real Android OAuth client (`...t2o7q.apps.googleusercontent.com`), tied to package `com.memeversestudio.app` + a specific SHA-1 fingerprint registered in Cloud Console. **Since `EXPO_PUBLIC_*` vars are compiled in at build time, this needs a real `eas build` (not just a Metro/dev-client reload) before it takes effect** — and the SHA-1 registered against this client must match the keystore of whichever build (debug vs. EAS-managed preview/release) is actually used to test, or Google rejects sign-in at the consent step before the app ever sees a token. iOS client ID still unset.
  - **Android client setup steps** (for reference / iOS's equivalent later): creating the Android OAuth client in Google Cloud Console asks for: **Package name** — `com.memeversestudio.app`. **SHA-1 certificate fingerprint** — from the signing key EAS manages (`npx eas-cli credentials`, run from `frontend/`, needs `npx eas-cli login` first — plain `eas` isn't a global command in this environment; pick Android → the build profile being tested → view/download the keystore, which prints the SHA-1 directly). Debug and release builds have different keystores/fingerprints — a production Play Store release re-signs with Play App Signing, which has its own fingerprint visible in Play Console → Setup → App integrity, and that one needs registering too before a Play-distributed build's Google Sign-In will work. **"Verify App Ownership"** — Google confirming the Cloud Console project owner also controls the Play Console listing for that package name; done by linking the Play Console account under Cloud Console → APIs & Services → OAuth consent screen, not something generated locally.
  - **Not yet exercised end-to-end** — no real Google OAuth client IDs exist to test against in this session (needs the user's Google Cloud Console setup, same blocker as F-1's Gmail credentials). Verify the whole flow — prompt → consent → token → account creation/login — on a real device once configured, before relying on it.
  - Tests: `tests/test_google_auth.py` (10 tests) mocks `google_auth_service._fetch_google_tokeninfo` (isolated specifically so the real `aud`/`email_verified` validation logic in `_verify_google_id_token` still gets exercised for real, not just the happy path) and monkeypatches `settings.google_signin_client_ids` per test.
- **F-12 (encryption at rest)**: written as `DATA_PROTECTION.md` at the repo root (not memory — it's a durable project-level checklist, same tier as `Idea.md`/`Project_Requirements.md`), per the audit's own "not application-level field encryption, write a one-page note" guidance. Covers Postgres/Redis/Cloudinary/transport encryption-at-rest and network-isolation items, plus the explicit reasoning for **not** doing application-level field encryption on `messages.body`. Every item in it is **unverified** — no deployment target exists in this repo to check any box against.
- **Declined**: F-9 (LLM daily spend budget) — user said skip it, AI features should stay unlimited. Do not add without asking again.
- **Not yet started**: F-10 (session visibility/revocation — explicitly deferred by user, matches the audit's own "not urgent" call). That's the last item from the approved EXPECTED+RECOMMENDED scope.

## Status
Testing. Security, performance, and accessibility sub-passes done. Automated regression done (full backend suite, 145/145, green with every Phase 16 change applied together). The manual half of the exit test — re-running Phases 1-15's actual exit tests by hand on a live device/simulator — is the one remaining item before Phase 16 can flip to Done; see Project_Timeline.md's Phase 16 entry for the walkthrough.

## Security review (done)
Ran a full audit of every router/service against auth, audience/community access-control, and IDOR risk (see `.claude/memory/messaging.md` for the one real bug found — that file replaced `meme-sending.md` in Phase 19, and the fix now lives on the messaging send path). Auth coverage, community membership gating, and owner-only mutation checks were already solid across all 15 prior phases — no other IDOR/access-control gaps found.

### Rate limiting (new — didn't exist before this phase)
- New `app/core/rate_limit.py`: a `slowapi.Limiter` keyed by **authenticated user ID** (decoded from the bearer token, when present) so limits track the account rather than a shared/rotating IP; falls back to client IP only for the pre-auth `register`/`login` endpoints. Storage backend is `settings.redis_url` (`storage_uri=`) — **this is the first real usage of Redis anywhere in the codebase**; every prior phase computed things live in SQL or in-process, so the `redis` Python package (listed in `requirements/base.txt` since Phase 0 but never actually installed/exercised) had to be installed for real. Wired into `app/main.py` via `app.state.limiter` + `SlowAPIMiddleware` + the `RateLimitExceeded` exception handler (429 responses).
- Limits applied (per-user unless noted, all `@limiter.limit(...)` decorators on the route function, which must take a `request: Request` param for slowapi to key off):
  - `POST /auth/register` — 5/minute (IP-keyed)
  - `POST /auth/login` — 10/minute (IP-keyed)
  - `POST /ai-caption/generate` — 15/minute (real billed Groq call per request)
  - `POST /competitions/{period}/votes/{meme_id}` and `.../container-votes/{id}` — 30/minute each
  - `POST /memes` (upload) — 20/minute
  - `POST /memes/{id}/reactions` — 60/minute
  - `POST /memes/{id}/comments` — 30/minute
  - `POST /meme-sending/send` — 30/minute
  - `POST /instagram/containers` — 10/minute (each call spawns a fire-and-forget background metadata-fetch task, so this is capped tighter than typical writes)
- **Test isolation**: rate-limit counters live in Redis, not the per-test Postgres schema `_reset_schema` fixture already resets — without a matching reset, counters leak across tests that share the same key (e.g. many tests share `127.0.0.1` as the IP-fallback key). Fixed with a new autouse `_reset_rate_limits` fixture in `conftest.py` that calls `limiter.reset()` before every test.
- Not rate-limited (deliberately): read-only GETs (feed, leaderboards, standings) and challenge-lifecycle mutations (setup/submit/accept/decline) — these are naturally infrequent/owner-gated and not realistic abuse vectors.

### CORS
- `app/main.py`'s `CORSMiddleware` no longer uses `allow_origins=["*"]`. Now reads `settings.cors_origins` (new `CORS_ALLOWED_ORIGINS` env var, comma-separated, parsed via a `cors_origins` property on `Settings` in `core/config.py`), defaulting to Expo's local web dev ports (`http://localhost:8081`, `http://localhost:19006`) so local dev needs no `.env` change. Also added `allow_credentials=True`. Set `CORS_ALLOWED_ORIGINS` explicitly in `.env` before any non-local deployment — a wildcard origin with credentials is invalid per the CORS spec anyway (browsers reject it), so this was a correctness fix as much as a security one.

## Key files
- `backend/app/core/rate_limit.py` (new), `backend/app/core/config.py` (`cors_origins`), `backend/app/main.py` (CORS + slowapi wiring), `backend/tests/conftest.py` (`_reset_rate_limits` fixture), `backend/.env.example`.
- Touched for the IDOR fix: `backend/app/services/meme_sending.py`, `backend/tests/test_meme_sending.py`.

## Tests
- Full backend suite (145/145) passing with rate limiting + CORS changes + the IDOR fix all live, against real Postgres + real Redis.

## Performance pass (done)
Reviewed indexes on every table touched by the feed/leaderboard/challenge/competition hot-path queries (`services/memes.py::meme_visibility_clause`, `services/leaderboards.py`, `services/challenges.py`, `services/competitions.py`) — most FK columns were already indexed from earlier phases, but a few real gaps existed:
- `memes.created_at` had **no index** despite being the feed's primary sort key (keyset pagination orders by `(created_at desc, id desc)`) — added a composite `ix_memes_created_at_id`.
- `community_memberships.status` had no index, despite `status == active` being filtered on every single community-scoped access check (`require_active_membership`, called from templates/feed/leaderboards/challenges — the single hottest lookup in the app) — added a composite `ix_community_memberships_community_user_status (community_id, user_id, status)` covering the whole check in one index.
- `friendships.status` had no index, despite being filtered in the feed visibility clause's two correlated EXISTS subqueries (friend-of-author, checked per candidate meme) — added `ix_friendships_requester_status` / `ix_friendships_addressee_status`.
- `challenge_participants.user_id` and `challenge_submissions.{meme_id,submitter_id}` had no index at all (not even via a FK — Postgres doesn't auto-index FKs) — added `index=True` to each.
- `post_audiences.audience_type` looked unindexed on paper, but its two existing **partial unique indexes** (`uq_post_audience_public_friends` on `(meme_id, audience_type) WHERE audience_type != 'community'`, `uq_post_audience_community` on `(meme_id, community_id) WHERE audience_type = 'community'`) already cover the visibility clause's actual query shapes — no new index needed there, confirmed by reading the actual predicates rather than trusting a flat column-index count.
- New migration: `alembic/versions/6b07283419b0_add_performance_indexes_for_feed_.py` (autogenerated, applied to the real dev DB). All 7 indexes are pure additions — no data migration, no behavior change, so no new tests were needed (existing 145/145 still pass, confirming query correctness is unaffected).

### Seeded-load timing
Ran a one-off script (not committed — throwaway, seeds via the real app/services against the dedicated `meme_platform_test` DB, not real dev data) with slowapi disabled for the run: 60 users, 5 open communities (all users joined to all 5), a friend chain (~5 friends/user), 480 memes (mixed public/friends/community audiences), reactions on 1/4 of memes (20 reactors each) and comments on 1/6 (5 commenters each). Timed cold (first-call, no warm cache) latency post-migration:

| Endpoint | Latency |
|---|---|
| `GET /memes/feed` (limit=20) | 299 ms |
| `GET /memes/feed` (limit=50) | 507 ms |
| `GET /communities/{id}/feed` | 184 ms |
| `GET /leaderboards/individual` | 202 ms |
| `GET /leaderboards/communities` | 185 ms |
| `GET /communities/{id}/leaderboard` | 292 ms |
| `GET /competitions/day/current` | 279 ms |

All well under 1s at this scale (reasonable for an MVP's expected initial user base), so judged **acceptable** for the Phase 16 exit test as written ("holds acceptable latency under seeded load"). The `limit=50` feed page is the one endpoint that scales noticeably worse than the rest (~1.7x the `limit=20` page rather than the expected ~2.5x row-count increase, actually sublinear — but still the slowest absolute number) because `build_meme_out` walks each meme's `selectin`-loaded `audiences` relationship in Python for every row; if feed latency becomes a real problem at higher scale (thousands of concurrent users, not the tens-of-thousands-of-rows tested here), that per-row Python-side loop plus the live reaction/comment COUNT subqueries per row (`services/memes.py::_paginated_feed`) are the next things to look at — likely a stored/cached count rather than a live aggregation, mirroring the same "live SQL now, cache later" trajectory already taken for [[scoring-engine]].

## Accessibility pass (done)
Frontend had never had an accessibility pass — `frontend/CLAUDE.md` mandates accessible labels/roles/touch targets as a directive, but it wasn't consistently applied. Audited the 5 screens the Phase 16 exit test names (Feed, Creator, Communities, Leaderboards, Profile) and fixed the real gaps found (most primary action buttons already had `accessibilityRole`/`accessibilityLabel` from earlier phases — the gaps clustered in a few recurring patterns):
- **Decorative avatar/icon `View`/`Image`s** (username initials circles, community icon fallbacks) had no `accessibilityElementsHidden`/`importantForAccessibility="no-hide-descendants"`, so a screen reader would announce raw initials text ("AL", "BE") with no context. Fixed across `SessionScreen.tsx` (Profile), `MemeCard.tsx` (Feed), `CommunityCard.tsx` (Communities), and both leaderboard row components.
- **Meme/preview images had no accessible label at all** — the actual content of the app was invisible to a screen reader. Fixed in `MemeCard.tsx` (feed image, labeled with caption or author), `OverlayCanvas.tsx` (in-progress edit), and `CreatorScreen.tsx` (captured preview).
- **Toggle/selection state not exposed via `accessibilityState`** — reaction hearts, Meme-of-the-Day vote buttons, tab toggles (Leaderboards' Individual/Communities, Communities' Mine/Discover), and the Create-Community privacy radio options all conveyed "selected" purely via background/border color, with the same label announced regardless of state. Added `accessibilityState={{selected/checked}}` throughout, plus switched tab Pressables to `accessibilityRole="tab"` and the privacy options to `accessibilityRole="radio"` (were generic `"button"`).
- **Leaderboard rows read as 3-4 disjoint text fragments** (rank, avatar, name, score all separate `Text` nodes with no grouping) — added a single `accessibilityLabel` (e.g. "Rank 3, jdoe, 420 points") on the wrapping `View` with `accessible` set, in both `IndividualLeaderboardRow.tsx` and `CommunityLeaderboardRow.tsx`.
- **`SessionScreen.tsx` (Profile) had zero accessibility props anywhere** — the clear outlier among the 5 screens; every nav button (Feed/Friends/Communities/Leaderboards/Voting/Inbox/Log out) got `accessibilityRole="button"` + a label, plus `accessibilityRole="header"` on the username.
- **Flagged, not fixed**: `DraggableText.tsx` (the Creator's drag-to-position top/bottom text overlay) has no non-visual alternative to dragging — a screen-reader/switch-control user cannot reposition text at all. Added `accessibilityRole="adjustable"` + a label/hint describing current text and that it's draggable, but a real fix (e.g. stepper buttons or a position picker) is a UI feature addition beyond a hardening-pass label/role fix, and wasn't built here — flagged for a follow-up decision, not silently addressed.
- Verified clean after all edits: `tsc --noEmit` (0 errors), `expo lint` (0 errors, same 2 pre-existing React Compiler/`react-hook-form` `watch()` warnings as every prior phase), `expo export --platform web` (17 routes, unchanged).

## Key files (accessibility)
- `frontend/src/features/auth/SessionScreen.tsx`, `frontend/src/features/feed/components/MemeCard.tsx`, `frontend/src/features/creator/components/{DraggableText,OverlayCanvas}.tsx`, `frontend/src/features/creator/CreatorScreen.tsx`, `frontend/src/features/communities/{CommunitiesScreen,CreateCommunityScreen}.tsx`, `frontend/src/features/communities/components/CommunityCard.tsx`, `frontend/src/features/leaderboards/LeaderboardsScreen.tsx`, `frontend/src/features/leaderboards/components/{IndividualLeaderboardRow,CommunityLeaderboardRow}.tsx`.
