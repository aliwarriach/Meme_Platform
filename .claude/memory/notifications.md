# notifications

## Status
Done (Phase 21, 2026-08-06; extended 2026-08-31 — "wave 2"). Backend: wave-1 223/223 full pytest suite against real Postgres; wave-2's own new tests (`test_notifications_wave2.py` 12/12, `test_notification_crons_wave2.py` 4/4) pass, plus the pre-existing friends/communities/memes/competitions/challenge-cron suites re-run clean after the new hooks were added. Frontend `tsc` clean. Human tap-through on a real device (push permission prompt, actual delivery) not yet done — needs a physical-device dev-client/standalone build, not the Expo Go/web dev flow (`Device.isDevice`/web checks in `pushNotifications.ts` skip registration on both).

Wave 1 shipped alongside 1v1 duels (see [[challenges]] Phase 21 section) — duels are the concrete "challenge-invite" event this system delivers, pulled forward from Phase 20's deferred §3.4. **Wave 2 (2026-08-31)** extended the same infra to every other feature that had zero notification coverage — user audit found `notify_one`/`notify_many` had exactly one caller (`services/challenges.py`) despite the generic infra supporting any feature. Scoped with the user via explicit menu selection: **in scope** — competition wins, friend requests (received+accepted), community join requests+resolutions, community-owner post moderation, meme comments, batched meme upvotes. **Explicitly out of scope, by user's own architectural reasoning** — "meme sent to you" (deliberately stays push-only via messaging's own unread-inbox surface, see Business rules below; re-adding a `Notification` row would recreate the exact duplicate-unread-concept problem wave 1 avoided). **Also out of scope, flagged not built**: "community moderation: membership removed by owner" — audited during wave 2 and found this feature doesn't exist in the backend at all (only self-leave and pending-request-rejection exist, no owner-initiated kick of an *active* member) — only the post-removal half of that original ask was real, so only that half was wired up.

## Two delivery channels, one call
`services/notifications.py::notify_one`/`notify_many` insert `Notification` row(s), push a `{"type":"notification", notification}` WS frame over the **existing single per-user socket** (`connection_manager`, same one messaging uses — no new connection), and enqueue an arq push-send job. Every challenge-lifecycle event goes through this. **New chat messages deliberately do NOT** — see Business rules.

## Models
- `Notification` (`app/models/notification.py`), table `notifications`: `user_id` (FK CASCADE, indexed), `type` (enum `NotificationType` — wave 1: `challenge_invite`, `challenge_invite_accepted`, `challenge_invite_declined`, `challenge_starting`, `challenge_ending_soon`, `challenge_side_overtaken`, `challenge_results`; **wave 2 (2026-08-31)**: `friend_request_received`, `friend_request_accepted`, `community_join_request`, `community_join_approved`, `community_join_rejected`, `community_post_removed`, `meme_comment_received`, `meme_upvotes_received`, `competition_won`), `title` (String 150), `body` (String 280), `data` (JSON — deep-link payload, shape varies by type: `challenge_id` / `conversation_id` / `friendship_id` / `community_id` / `meme_id` / `period_type`+`period_key`), `read_at` (nullable). Composite index `(user_id, created_at, id)` backs keyset pagination, same shape as `messages`.
- `PushToken` (same file), table `push_tokens`: `user_id` (FK CASCADE, indexed), `token` (**globally unique**, not unique-per-user — a device's token must move to whoever re-registers it, e.g. a different account logging into the same phone), `platform` (String 16, e.g. `"ios"`/`"android"`).
- **`CompetitionWinnerNotification`** (`app/models/competition_winner_notification.py`, wave 2), table `competition_winner_notifications`: `period_type` (Postgres enum `competition_period_type` — recreated fresh by this migration; the original enum of this name was fully dropped when [[instagram-companion]]'s `ContainerVote` redesign removed period-scoped voting, so this isn't reusing an existing DB type) + `period_key` (String 16), `UniqueConstraint(period_type, period_key)`. Pure dedup ledger — see Business rules.
- **`NotificationCronCursor`** (`app/models/notification_cron_cursor.py`, wave 2), table `notification_cron_cursors`: `job_name` (String 64, PK) + `last_run_at`. One row per *recurring batching* cron (currently just `meme_upvotes_batch`) — distinct from `Challenge.ending_soon_notified_at`'s one-shot-per-row flag pattern, since a batching job sweeps a moving time window across many rows each run and needs its own bookmark.

## Endpoints
All under `/notifications`, Bearer-auth-gated.
- `GET /notifications?cursor=&limit=` → `200` `{items: [NotificationOut], next_cursor}` — newest-first keyset, same cursor helper as messaging.
- `GET /notifications/unread-count` → `200` `{count}`.
- `POST /notifications/{id}/read` → `200` `NotificationOut`. `404` (not 403) if the notification doesn't belong to the caller — ids aren't enumerable, and confirming existence to a non-owner has no legitimate use either.
- `POST /notifications/read-all` → `200` `{read_count}`. Idempotent (`0` when nothing was unread).
- `POST /notifications/push-token` body `{token, platform}` → `204`. **Upsert by token**, not by (user, token) — see Models.
- `DELETE /notifications/push-token?token=` → `204`. `token` is a **query param**, not a JSON body field (same `meme_id`-on-submissions precedent — DELETE-with-body is avoided).

## Business rules
- **New messages are push-only, never a `Notification` row — still true after wave 2, deliberately.** `services/messaging.py::send_message` checks `connection_manager.send_json(...)`'s return value (renamed `delivered_live`) — if the recipient isn't connected, it enqueues the same `send_push_job` arq job directly, bypassing `notify_*` entirely. Reason: a conversation already has its own unread-count/inbox surface (see [[messaging]]); a duplicate `Notification` row would be a second, redundant unread concept. This is why "meme sent to you" was excluded from wave 2's scope even though it was flagged as a gap in the initial audit — a meme send is just a `kind="meme"` message on the same pipe, the same reasoning applies unchanged.
- **Challenge-lifecycle hook points** (in `services/challenges.py`) are listed in [[challenges]]'s Business rules — this file only owns the notification *infrastructure*, not which events fire it.
- **Wave 2 event-driven hook points** (call `notify_one` right after their own `commit()`+`refresh()`, matching the wave-1 challenges.py convention exactly):
  - `services/friends.py::send_friend_request` → `friend_request_received` to the addressee; `accept_friend_request` → `friend_request_accepted` to the original requester.
  - `services/communities.py::join_community` → `community_join_request` to the owner, **only** when the new membership lands as `pending` (invite-only community, no existing invite to flip) — an **open** community's immediate self-join is not a moderation event and stays silent. `approve_join_request`/`reject_join_request` → `community_join_approved`/`community_join_rejected` to the requester; both re-fetch the `Community` via `db.get()` after `_get_pending_request_or_404` already loaded it in the same session (identity-map hit, not a second query) to get its name for the copy.
  - `services/memes.py::delete_meme` → `community_post_removed` to the author, **only** on the `is_community_owner AND author != current_user` branch (moderation) — an author deleting their own post never self-notifies.
  - `services/comments.py::add_comment` → `meme_comment_received` to the meme's author, **only** when the commenter isn't the author (no self-notify). Body is the comment text truncated to 150 chars + `…` (helper `_COMMENT_PREVIEW_MAX_CHARS`), safely under `Notification.body`'s 280-char column even though comments allow up to 500.
- **Two new arq crons for events with no service-layer hook point to fire from** (both registered in `arq_worker.py`, both poll every 15 min):
  - **`notify_competition_winners`** (`app/workers/tasks/notifications.py`) — there's no "a period just closed" event since [[voting-system]]'s standings are computed live on read (no snapshot/recompute worker), so this polls all three `CompetitionPeriod` values every run, derives each one's *immediately-preceding* period key (`period_key(type, current_period_start - 1s)`), and claims it via `CompetitionWinnerNotification`'s `ON CONFLICT DO NOTHING` **before** resolving/notifying — so a crash mid-notify or an overlapping run can never double-send (worst case: a period is marked "handled" with nothing actually sent, treated as an acceptable one-time miss vs. a recurring duplicate). The recipient is resolved by `services/competitions.py::get_winner_recipient` — a new function that reads the raw `Meme`/`MemeContainer` row directly (bypassing `_standings_query`'s `meme=None, is_deleted=True` API-facing degradation) so the winner is still notified even if they deleted their winning post between period-close and this cron's next run. `_standings_query`'s ranking logic was extracted into a shared `_ranked_content_ids` helper so both paths use identical ranking without duplicating it.
  - **`notify_batched_meme_upvotes`** — deliberately NOT event-driven off `cast_vote`: a per-vote notification would spam a popular meme's author (this batching-vs-per-vote choice was put to the user explicitly and confirmed). Tracks its own sweep window via `NotificationCronCursor` (`job_name="meme_upvotes_batch"`, first run defaults to `now - 15min`), groups new upvotes (`value=1`, `created_at` in `(cursor, now]`, `Meme.deleted_at IS NULL`) by meme, **excludes the meme's own author's votes** (self-upvoting shouldn't count toward "you got upvoted" — memes have no self-vote block elsewhere in the app, unlike containers' documented allow-self-vote precedent, so this exclusion is local to this cron only), and sends one `meme_upvotes_received` per meme with `body="+N new upvote(s) since your last check."`.
  - Both wave-1 crons — `notify_challenges_ending_soon` (every 5 min — active challenges with `end_time` inside the next 60 min and `Challenge.ending_soon_notified_at IS NULL`, one-shot via that flag), `notify_side_overtaken` (every 60s — recomputes `_side_scores`, compares the argmax side against `Challenge.leading_side_id`, only notifies when **both** old and new leader are non-null and different — see [[challenges]] Business rules for the missing-side-defaults-to-0.0 bug this surfaced during testing) — and `create_weekly_open_challenge` (Monday 00:00 UTC — cold-start platform challenge, see [[challenges]]) are unchanged by wave 2.
- **Push delivery is fire-and-forget and never raises** — `app/integrations/expo_push.py::send_push_notifications` catches `httpx.HTTPError` and only logs; a flaky Expo API must not fail the request or job that triggered it (backend/CLAUDE.md's background-task directive). No `exponent_server_sdk` dependency — a raw `httpx` POST to `https://exp.host/--/api/v2/push/send`, batched at 100 messages/request (Expo's cap), same precedent as `integrations/llm_client.py`'s raw call to Groq over a heavier SDK.
- **The push-send job (`app/workers/tasks/notifications.py::send_push_job`) is a real arq job**, registered in `WorkerSettings.functions` — `notify_one`/`notify_many` and the offline-message path both enqueue it by name via `get_arq_pool()`, never call it directly, so it runs on the worker process like every other background task in this repo.

## Frontend integration notes
- `services/notifications.ts` (REST + wire types) · `services/notificationsCache.ts` (**pure, unit-tested cache-patch helpers** — insert/mark-read/mark-all-read, never invalidate) · `services/useNotifications.ts` (TanStack Query hooks + `useNotificationsSocketSync()`, mounted once at the app root exactly like `useMessagingSocketSync()`).
- Query keys: `['notifications','list']` (infinite) and `['notifications','unreadCount']`. A live `notification` socket frame patches the list cache **and** bumps the unread-count cache directly (no refetch) — same "patch don't invalidate" rule as [[optimistic-cache]] and [[messaging]].
- `services/pushNotifications.ts::getExpoPushTokenAsync()` — resolves `null` (not an error) on web, simulator/emulator, or denied permission; callers must treat `null` as "skip registration." Uses the EAS `projectId` already in `app.json`'s `extra.eas`.
- Push registration lives directly in `app/_layout.tsx`'s `AuthBoundary`, as a second effect keyed on the auth `token` right next to the existing socket-connect effect (not a hand-rolled hook — matches the existing `connectMemeSendingSocket`/`disconnectMemeSendingSocket` pattern of plain functions called from `_layout.tsx`). Registers on login, unregisters (via a `useRef`-held last-known Expo token) on logout.
- `components/NotificationBell.tsx` — icon + unread badge, dropped into `TopBar`'s existing `rightActions` slot on the feed screen (the highest-traffic one); `TopBar` needed no changes, the slot already existed for the inbox icon.
- Route: `/notifications` (`features/notifications/NotificationsScreen.tsx`) — infinite `FlatList`, tap marks read and deep-links by checking `data` keys in order: `challenge_id` (→ `/challenges/[challengeId]`), `conversation_id` (→ `/inbox/[conversationId]`), **wave 2:** `meme_id` (→ `/memes/[id]`), `community_id` (→ `/communities/[id]`), `friendship_id` (→ `/friends`, no per-request detail screen exists), `period_type` present (→ `/voting`). `ICON_BY_TYPE` and `NotificationType`/`NotificationData` (`services/notifications.ts`) extended for all 9 new types/keys.
- **2026-09-01 UX-audit fix — the two deep links that need the user to land on a specific *state*, not just the right screen:**
  - `community_join_request` → `/communities/[id]?tab=members&openRequests=1`. `CommunityDetailScreen.tsx` reads `tab`/`openRequests` via `useLocalSearchParams` and seeds `activeTab`'s and `requestsOpen`'s initial `useState` values from them (lazy initializer, not a `useEffect`+`setState` — that combo trips the `react-hooks/set-state-in-effect` eslint rule; `openRequests` doesn't wait on `isOwner` resolving first since `CommunityRequestsModal` is already owner-gated server-side and shows a normal inline error for a non-owner, never a crash). This reaches the exact same state the pre-existing pending-requests badge already opens (`setActiveTab('members'); setRequestsOpen(true)`), confirmed by reading that badge's own handler rather than trusting this file's older "jumps to the Members tab" note (that description undersold what the badge actually does — it opens `CommunityRequestsModal` directly, not just the tab).
  - `competition_won` → `/voting?period=<period_type>`. Both `VotingScreen.tsx` (native) and `VotingScreen.web.tsx` independently read `period` via `useLocalSearchParams` and seed `activeTab`'s initial value with it (falls back to `'day'` if missing/invalid) — without this a week/month win landed on the Today tab, where `useWinner(activeTab, ...)` never renders that win at all. Duplicated per-file rather than extracted, matching these two screens' existing pattern of independent implementations.
  - Also fixed the same pass: the notifications-list empty-state copy ("Nothing yet — challenge invites, results, and updates will show up here") was stale after wave 2 added five more categories — reworded to mention friend requests/community activity/comments generically instead of enumerating only challenges.
  - Found via `edge-case-ux-auditor` run on this turn's notification changes, not self-caught during the original wave-2 build.
- `expo-notifications` added as a dependency + config plugin in `app.json`. `Notifications.setNotificationHandler` (in `pushNotifications.ts`) shows a foreground banner — without it, a push arriving while the app is open is silently swallowed on some platforms.

## Gotchas
- **`tests/fake_arq.py`'s `_JOB_FUNCTIONS` dict must list every job name a test path can enqueue** — `send_push_job` had to be added there, or any test triggering a notification would `KeyError` inside `FakeArqPool.enqueue_job` (jobs run inline in tests, no real worker — see [[redis-arq-infra]]).
- **`get_arq_pool` is patched per-module in `tests/conftest.py`'s `use_fake_arq_pool` fixture, not globally** — `services/notifications.py` and `services/messaging.py` (the offline-push path) both needed their own `monkeypatch.setattr(..., "get_arq_pool", get_fake_arq_pool)` line added, or they'd try a real Redis connection during tests.
- **The real Expo HTTP call also needed mocking separately from the arq pool** — `FakeArqPool` runs `send_push_job`'s body for real (by design, to exercise real logic), which would otherwise fire an actual request to `exp.host` on every test that triggers a notification. New autouse fixture `mock_expo_push` in `conftest.py` patches `app.workers.tasks.notifications.send_push_notifications` (patched where it's *called*, not where it's defined — same rule as `mock_media_upload`).
- **`app/workers/tasks/notifications.py::send_push_job` and the three new cron jobs all need `async_session_factory` patched to `TestSessionFactory`** in `conftest.py`'s `use_test_session_factory_for_background_tasks` fixture — they open their own DB session outside any request/dependency context, same pattern as the Instagram metadata job.

## Key files
- backend: `app/models/notification.py`, `app/models/competition_winner_notification.py` (wave 2), `app/models/notification_cron_cursor.py` (wave 2), `app/schemas/notifications.py`, `app/services/notifications.py`, `app/routers/notifications.py`, `app/integrations/expo_push.py`, `app/workers/tasks/notifications.py`, `app/workers/arq_worker.py`, `app/services/challenges.py` (wave-1 hook points), `app/services/friends.py` / `app/services/communities.py` / `app/services/memes.py` / `app/services/comments.py` (wave-2 hook points), `app/services/competitions.py` (`_ranked_content_ids` + `get_winner_recipient`, wave 2), `app/services/messaging.py` (offline-push hook), `app/core/exceptions.py` (`NotificationNotFoundError`), `alembic/versions/acb0458543db_create_notifications_and_push_tokens.py` (wave 1), `alembic/versions/584f3b9c7195_add_friend_community_engagement_.py` (wave 2 — 9 new enum values via hand-written `ALTER TYPE ... ADD VALUE IF NOT EXISTS`, same precedent as `9c2a5e14d7f1_add_invited_membership_status.py`, + the two new tables).
- tests: `backend/tests/test_notifications.py` (wave 1), `backend/tests/test_notifications_wave2.py` + `backend/tests/test_notification_crons_wave2.py` (wave 2), `backend/tests/test_challenge_notification_crons.py`, `backend/tests/conftest.py` (`mock_expo_push` + the `get_arq_pool`/`async_session_factory` patches above — wave 2's two new crons live in the same `app/workers/tasks/notifications.py` module already covered by these patches, no new fixture wiring needed), `backend/tests/fake_arq.py`.
- frontend: `src/services/{notifications,notificationsCache,useNotifications,pushNotifications}.ts`, `src/components/NotificationBell.tsx`, `src/features/notifications/NotificationsScreen.tsx`, `src/app/notifications.tsx`, `src/app/_layout.tsx` (push registration effect + socket sync mount), `src/services/memeSendingSocket.ts` (`notification` frame added to `IncomingMessage`), `src/features/communities/CommunityDetailScreen.tsx` + `src/features/voting/{VotingScreen.tsx,VotingScreen.web.tsx}` (2026-09-01, read the `tab`/`openRequests`/`period` params these deep-links now send).

## Tests
- `backend/tests/test_notifications.py` (11): push-token register is an idempotent upsert, a re-registered token moves to the new owner, unregister, keyset pagination, unread-count + mark-read (+ scoped-to-owner 404), mark-all-read + idempotency, real WS delivery of a `notification` frame (triggered via a duel invite).
- `backend/tests/test_challenge_notification_crons.py` — see [[challenges]] Tests section.
- **`backend/tests/test_notifications_wave2.py` (12, 2026-08-31)**: friend request received/accepted, invite-only join request notifies owner, open-community self-join does *not* notify owner, join approval/rejection notify the requester, community-owner post removal notifies the author, self-delete does *not* self-notify, comment notifies the meme author (+ body echoed verbatim), self-comment does *not* self-notify, long comment body is truncated to ≤151 chars with a trailing `…`.
- **`backend/tests/test_notification_crons_wave2.py` (4, 2026-08-31)**: competition-winner cron notifies the previous day-period's winner exactly once and is idempotent on a second poll (every period type's dedup row is claimed on first check regardless of outcome, so a re-poll within the same day/week/month always processes 0), the same cron still resolves/notifies correctly even when the winning post was deleted *after* the period closed (exercises `get_winner_recipient`'s raw-row bypass of the API's soft-delete degradation), batched-upvotes cron summarizes multiple upvotes into one notification and excludes the meme author's own self-upvote from the count, batched-upvotes cron ignores votes on a since-deleted meme.
- `frontend/src/services/notificationsCache.test.ts` (9): insert into empty/ordered/duplicate cases, page-reference preservation, mark-read (single + already-read no-op + unknown-id no-op), mark-all-read across multiple pages + no-op when nothing's unread.
